# 海川珠宝 — AI 统一执行流程（WORKFLOW）

> 本文件是唯一执行流程：说明何时直接做、何时暂停、怎样验证和怎样交付。
> 权限与审批只认 `AGENTS.md`；技术不变量见 `PROJECT_RULES.md`；本文件不重新定义权限。
> 最近核对：2026-09-05（区分实际与预期行为，统一暂停条件、独立复核与按任务类型交付）。

---

## 0. 决策顺序

1. **安全与授权**：先遵守 `AGENTS.md`。
2. **用户结果**：明确用户最终能观察到的结果、验收标准和不改范围。
3. **项目事实**：确认实际行为时，按实际运行 → 当前代码、类型、合同、Schema、配置 → 已核对状态文档 → 历史记录取证。预期行为与验收标准依据现行批准需求、合同和技术规则；运行中的 Bug 或旧构建不能覆盖这些依据。两者不一致时先核对版本、环境和数据来源。
4. **产品与品牌**：`docs/PROJECT_GUARDRAILS.md` 是唯一项目与品牌方向基线；产品定位、品牌表达、公开页面职责和客户旅程不能被技术便利或其他专项文档覆盖。
5. **工程实现**：选择最小但完整、可维护且可验证的实现。

## 1. 最小启动

- 实施任务先读 `AGENTS.md` 与本文件；只读 `PROJECT_RULES.md` 和 `docs/` 中与任务相关的章节，不循环加载全部治理文档。
- 任务依赖工作树状态或准备修改时，先汇总 `git status --porcelain`，再检查目标路径相关 diff；不输出全量状态或 diff。已有改动不覆盖、不回退、不混入。
- 在内部或简短更新中明确“目标 / 验收 / 不改范围”。任务已经清楚时不要求用户重复确认。
- 前后端/合同联动、数据库迁移、装修器端到端链路、共享架构、生产门禁或多个独立验收阶段属于复杂任务。实施前明确目标、验收、影响、风险和阶段；已获授权时直接推进，新证据推翻假设时更新同一方案。
- 证据明确则处理；证据不足只做能增加新信息或排除假设的最小取证。
- 大合同、生成文件、日志、diff 和浏览器树使用目标查询、脚本断言或摘要，不把原始大结果带入会话。

## 2. 直接执行与暂停

满足以下条件时直接执行：用户已明确要求结果、实现路径不会改变产品取舍、操作可逆，且操作不命中 `AGENTS.md` 的审批项，或当前及此前仍有效的明确请求已经精确授权该审批项的目标、范围和动作。用户已经给出的有效授权不得被改写成重复确认。

只有以下情况暂停：缺失信息会显著改变产品结果；需要当前授权未覆盖的新权限、外部协调或高风险执行；范围明显扩大；现有改动冲突；权威来源无法通过事实核验收敛。普通命名、局部布局和实现细节采用最小合理假设并在交付中说明；工具或平台要求的独立审批仍由对应机制处理。

实现任务的终止条件是：用户可观察结果已经实现，完成该结果所必需的调用链和失败路径没有已知缺口，并取得与风险相称的当前证据。已授权范围内的必需项不得留成“下一步建议”或新的确认点；只有真实阻断时才结束未完成部分，并继续完成所有未受阻范围。阶段只用于组织可独立验收结果，不能把同一闭环的保存、重渲染、兼容或目标验证推迟到无归属的后续阶段。

## 3. 按任务路由

- 产品定位、品牌原则、公开信息架构或页面职责 → 先读 `docs/PROJECT_GUARDRAILS.md`；其他文档不得另行定义方向。
- 客户前台视觉、交互、响应式 → 先确认 `docs/PROJECT_GUARDRAILS.md` 的页面目标，再按 `docs/UI_GUIDE.md` 落地；修改前看真实页面/截图，完成后做真实桌面与移动端验收。
- 管理后台视觉、交互、响应式 → `docs/UI_GUIDE.md` 附录 A 与 `docs/ADMIN_COPY_GUIDE.md`，按受影响页面、视口和状态做真实页面验收；不套用客户前台的展示型表达。
- 页面装修器与模板 → 先读 `PROJECT_RULES.md` §7 与 `docs/page-builder/template-design-framework.md`，再按问题分别核对结构合同、内置兼容合同、Template Repository 或 `PageDocument`；不得把其中任一来源扩张为全部模板事实。属性面板任务以用户可完成的端到端能力为范围，必要的合同、适配器、保存、发布和公开渲染接线应纳入同一闭环，但不得借机改造无关模块或编辑器外框。
- 当前功能开关、模板数量、内容完备度、迁移状态、测试快照或发布状态 → 先查 `docs/CURRENT_STATE.md`，再用当前代码、配置和实际运行结果复核；状态文档只提供证据，不授予操作权限，也不得覆盖产品、技术和安全权威。
- 接口、数据、权限或生产风险 → 读取对应技术规则和当前调用链；按第 2 节核对已有授权，缺少授权时给出方案、精确范围、回退和验证门禁。
- 纯解释、审计或状态报告 → 只读取回答所需证据，不因“了解项目”全量扫描仓库。

专项文档定义项目事实，Skill 定义可复用工作方法，两者不得互相复制。用户明确点名可用 Skill 时按要求使用；未点名时仅在该 Skill 的 `description` 与当前任务真实匹配时触发。公共前台视觉、品牌影像、系统调试、Playwright、部署、计划和完成前核验分别由当前可发现的对应项目 Skill 路由；法庭审校只有用户明确触发时才读取 `.agents/skills/critical-review/SKILL.md`，高风险本身只决定证据、授权和回退要求，不自动改变回答形式。普通小改动不为形式追加法庭、完整计划、多智能体或全仓验证。

阶段计划、审计清单和旧对话中的 P0/P1/P2/P3 只约束其当次任务，不是永久产品规则。用户已明确更新目标时，以当前批准决策和现行规则重新划分实施批次；不得用旧阶段的文件范围阻止新需求的必要链路，也不得以“新需求”为由绕过 `AGENTS.md`、真实数据、单一事实源、品牌、安全、权限和发布门禁。

用户明确批准新的产品或品牌方向后，先更新 `docs/PROJECT_GUARDRAILS.md`，再同步受影响的专项文档和实施批次；同步完成前，旧决策或状态文档不得覆盖新的方向。

## 4. 实施边界

- 只修改已授权目标，优先修根因和共享入口。验证暴露的既有失败若与受影响子系统相关或直接阻断本任务验证，且原因明确、低风险、范围小、可验证、不覆盖现有修改，也不改变产品、架构或数据行为，可作为同一主方案的清理步骤修到通过；不得通过删除或跳过测试、降低规则或随意改写期望值制造绿色结果。系统性、跨系统、高风险、环境或边界不清的问题只记录并升级为独立阶段；构成实质扩展范围时先请求授权。
- 每轮检查必须增加证据或排除假设；连续两轮无新证据时停止重复扫描并报告下一项最有价值的验证。
- 暂停统一按第 2 节判断：权限冲突、范围显著扩大或现有改动冲突时停止受影响的写操作；权威来源冲突经必要事实核验仍无法收敛时再请求决定。可以继续不受影响的工作和只读取证。

## 5. 风险分级验证

先确定准备声明的完成层级，再选择证据；不同层级不能互相替代：

单项或阶段验收只证明其明确范围。整站成熟与可运营的总目标以 `docs/PROJECT_GUARDRAILS.md` 第 2 节为准，汇总入口为 `docs/acceptance/feature-completion-matrix.md`；不得把最小公开版本、局部测试或单个编辑器闭环通过外推为整站合格，也不得因此给无关局部任务追加全站验收。

| 完成层级 | 可以声明的结果 | 不能替代 |
| --- | --- | --- |
| 本地实现 | 当前代码、合同、目标测试、Mock/夹具或本地 Renderer 已证明范围内实现闭环；没有已知的本地必需接线遗漏 | 真实 API、数据库、权限、发布或生产 |
| 真实联调 | 指定非生产环境中的真实 API、持久化、权限和重载链路已经验证 | 发布版本、CDN、生产权限和回滚 |
| 上线 | 获批目标环境的发布、公开路由、缓存、监控和回退门禁已经完成 | 不能由本地、Mock 或单次构建推断 |

高一层未请求、未授权或环境不可达，不自动否定已经取得证据的低一层结果；但本地完成条件中的关键路径尚未验证时，只能写“已完成代码修改，待本地验收”。没有上线请求时写“上线状态：不在本次范围”，不要把它描述成本地实现的剩余步骤或上线阻断。

下表是各层级内的必需验证基线，不是完成上限。应执行所有与当前变更、目标层级相关且工具、权限和环境可达的验证；项目脚本、CI 门禁、影响范围或风险要求更多检查时继续执行。无法执行的必要层级必须标为未验证并说明原因。

| 改动类型 | 必需验证基线 |
| --- | --- |
| 文档、规则、提示词 | 路径与章节引用、重复与冲突、过时事实、正反场景推演、`git diff --check`；Skill 变更另核对应触发与不应触发提示 |
| 前端逻辑或类型 | 相关类型检查、构建或目标测试；覆盖受影响状态 |
| 后端接口 | 后端构建或目标测试；成功、失败、权限与边界路径 |
| 前后端契约 | 两端检查、合同或目标测试；目标包含真实持久化、已有可用联调环境或准备上线时，再做真实接口联调并注明环境 |
| 局部视觉或文案 | 受影响桌面/移动视口、内容与状态；不要求无关全站矩阵 |
| 组件布局 | 桌面、移动和受影响断点，检查受影响的溢出、键盘、对比度或加载稳定性 |
| 全局导航、令牌、页面骨架 | 完整代表性视口与关键状态矩阵 |
| 单个模板或兼容适配器 | 对应合同检查 + 受影响结构、内容状态、视口及编辑器/预览/公开端路径；不机械扩展到无关模板 |
| 模板结构能力或共享 Renderer | 对应节点/槽位类别、响应式边界、编辑器/页面/公开端和受影响兼容适配器；只有修改内置兼容合同或全目录行为时才全验对应兼容模板集合 |
| 属性面板字段、选择器或实例覆盖能力 | 本地实现至少验证控件状态、编辑与撤销/重置、可达环境中的保存请求与重载回显、画布预览、只读 Renderer 和兼容读取；异步或结构化对象只覆盖该能力实际存在的加载、空、错误、失效请求、无效引用与数量边界。任务包含真实持久化、发布或上线时，再追加对应层级的真实保存、发布成功与阻断、公开路由验证 |
| 数据库、交易、权限或生产 | 获批方案规定的成功、失败、并发、回退和目标环境门禁 |

验证证明什么就只声明什么。构建不证明视觉，Mock 不证明真实接口，本地成功不证明生产可用。原始大输出可以摘要，但必须保留命令、退出码和关键失败证据。

独立只读复核由变更风险决定：触及店铺装修共享模型、跨消费面 Renderer/Inspector 行为、草稿与发布生命周期、权限、数据、交易、发布、生产，或用户明确指定的关键里程碑时，需要未参与实现的验证者复核。可以使用内置只读子代理；普通局部、可逆工作由主任务按当前证据验收，无需额外复核者。单任务、子代理和独立任务采用相同风险门槛，不因需要复核就自动创建用户可见任务。

验证者只读取证，发现缺陷交回原写入者修复；主任务仍须核对复核证据并承担最终结论。工具、权限或环境不足以完成独立复核时，继续完成可达的实现和验证，分别报告其结果，将独立复核标为未完成，不宣称该门禁通过，也不据此执行依赖它的提交或发布。

## 6. 交付

- 实现、集成或发布任务按实际涉及的层级交付：结论、修改内容、验证证据、必要的本地实现/真实联调/上线状态，以及剩余风险。接口注明 Mock/真实，视觉注明实际视口与状态；纯解释和只读审查按结论、证据及必要建议交付，不强制套用实现状态栏目。简单任务可合并段落。
- “完成”只描述已验证层级；未执行、失败或环境不具备的必要检查必须明确说明。未请求的更高层验证写明“不在本次范围”，不得制造“代码只做了一半”的含义，也不得把它冒充已经完成。

## 7. 治理文件变更

- 只有用户明确要求时才能修改规则或决策；先检查职责、引用、重复、冲突和过时事实，再做最小一致改动。
- 新规则必须说明解决的风险、适用范围、验证方式和例外条件；口号、供应商瞬时事实和重复实现不得进入公共规则。
- 治理变更按影响范围做场景推演；涉及权威职责、安全、产品或品牌方向、发布门禁时，至少覆盖无关任务不受牵连、现行权威裁决旧记录、状态文档不产生授权，以及发现未经授权或敏感素材后先识别公开风险、再按 `AGENTS.md` 获得所需授权并通过可逆路径停止暴露和整改。纯文字或引用修正只需检查路径、重复、冲突和过时事实。
- 授权或执行流程变更还必须覆盖以下代表场景：只读检查直接进行；普通项目内实现不重复请示；用户已精确批准的受控操作直接进入执行；笼统请求不自动授权高风险动作；数据库、支付、生产和敏感信息仍受门禁；脏工作树重叠时暂停；本地项目 worktree 可用但网络共享仍拒绝。
