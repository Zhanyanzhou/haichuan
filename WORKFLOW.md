# 海川珠宝 — AI 统一执行流程（WORKFLOW）

> 本文件是唯一执行流程：说明何时直接做、何时暂停、怎样验证和怎样交付。
> 权限与审批只认 `AGENTS.md`；技术不变量见 `PROJECT_RULES.md`；本文件不重新定义权限。
> 最近核对：2026-08-20（解除循环读取，改为结果与风险驱动）。

---

## 0. 决策顺序

1. **安全与授权**：先遵守 `AGENTS.md`。
2. **用户结果**：明确用户最终能观察到的结果、验收标准和不改范围。
3. **项目事实**：实际运行 → 当前代码、类型、合同、Schema、配置 → 已核对状态文档 → 历史记录。
4. **产品与品牌**：产品行为、经营事实和品牌体验不能被技术便利覆盖。
5. **工程实现**：选择最小但完整、可维护且可验证的实现。

## 1. 最小启动

- 实施任务先读 `AGENTS.md` 与本文件；只读 `PROJECT_RULES.md` 和 `docs/` 中与任务相关的章节，不循环加载全部治理文档。
- 检查 `git status` 与相关 diff；已有改动不覆盖、不回退、不混入。
- 在内部或简短更新中明确“目标 / 验收 / 不改范围”。任务已经清楚时不要求用户重复确认。
- 证据明确则处理；证据不足只做能增加新信息或排除假设的最小取证。
- 大合同、生成文件、日志、全量 diff 和浏览器树使用目标查询、脚本断言或摘要，不把原始大结果反复带入会话。

## 2. 直接执行与暂停

满足以下条件时直接执行：用户已明确要求结果、实现路径不会改变产品取舍、操作可逆、且不命中 `AGENTS.md` 的审批项。

只有以下情况暂停：缺失信息会显著改变产品结果；需要新的权限或外部协调；命中高风险审批；范围明显扩大；现有改动冲突；权威来源无法通过事实核验收敛。普通命名、局部布局和实现细节采用最小合理假设并在交付中说明。

## 3. 按任务路由

- 客户前台视觉、交互、响应式 → `docs/UI_GUIDE.md`，修改前看真实页面/截图，完成后做真实桌面与移动端验收。
- 管理后台视觉、交互、响应式 → `docs/UI_GUIDE.md` 附录 A 与 `docs/ADMIN_COPY_GUIDE.md`，按受影响页面、视口和状态做真实页面验收；不套用客户前台的展示型表达。
- 页面装修器与模板 → `PROJECT_RULES.md` §7、`docs/page-builder/template-design-framework.md` 与当前机器合同；架构历史按需查 `docs/DECISIONS.md` A.6/A.9/D.8/D.17，编辑器、预览和公开 Renderer 不得建立平行规则。属性面板任务以用户可完成的端到端能力为范围，不以 Inspector 目录或表面控件为文件边界；必要的合同、适配器、保存、发布和公开渲染接线应纳入同一闭环，但不得借机改造无关模块或编辑器外框。
- 接口、数据、权限或生产风险 → 读取对应技术规则和当前调用链；命中审批时先给方案、精确范围、回退和验证门禁。
- 纯解释、审计或状态报告 → 只读取回答所需证据，不因“了解项目”全量扫描仓库。

阶段计划、审计清单和旧对话中的 P0/P1/P2/P3 只约束其当次任务，不是永久产品规则。用户已明确更新目标时，以当前批准决策和现行规则重新划分实施批次；不得用旧阶段的文件范围阻止新需求的必要链路，也不得以“新需求”为由绕过 `AGENTS.md`、真实数据、单一事实源、品牌、安全、权限和发布门禁。

## 4. 实施边界

- 只修改授权范围，优先修根因和共享入口；范围外问题只记录。
- 每轮检查必须增加证据或排除假设；连续两轮无新证据时停止重复扫描并报告下一项最有价值的验证。
- 出现权限冲突、范围显著扩大、现有改动冲突或权威来源不一致时停止，请求决定。

## 5. 风险分级验证

| 改动类型 | 最低验证 |
| --- | --- |
| 文档、规则、提示词 | 路径与章节引用、重复与冲突、过时事实、场景推演、`git diff --check` |
| 前端逻辑或类型 | 相关类型检查、构建或目标测试；覆盖受影响状态 |
| 后端接口 | 后端构建或目标测试；成功、失败、权限与边界路径 |
| 前后端契约 | 两端检查、合同或目标测试、真实接口联调并注明环境 |
| 局部视觉或文案 | 受影响桌面/移动视口、内容与状态；不要求无关全站矩阵 |
| 组件布局 | 桌面、移动和受影响断点，检查受影响的溢出、键盘、对比度或加载稳定性 |
| 全局导航、令牌、页面骨架 | 完整代表性视口与关键状态矩阵 |
| 单个 Puck 模板 | 机器合同验证 + 全验受影响模板在编辑器、预览与公开端的受影响视口 |
| 共享 Puck Renderer、合同结构、顺序或高度 | 机器合同全量验证 + 所有活跃模板真实浏览器自动 DOM/顺序/高度检查；人工视觉按风险抽样 |
| 属性面板字段、选择器或实例覆盖能力 | 验证控件状态、编辑与撤销/重置、草稿保存、刷新回显、画布预览、发布成功与阻断、公开渲染和旧文档默认兼容；结构化对象还要覆盖加载、空、错误、分页/筛选、失效请求、无效引用与数量边界 |
| 数据库、交易、权限或生产 | 获批方案规定的成功、失败、并发、回退和目标环境门禁 |

验证证明什么就只声明什么。构建不证明视觉，Mock 不证明真实接口，本地成功不证明生产可用。原始大输出可以摘要，但必须保留命令、退出码和关键失败证据。

## 6. 交付

- 交付使用：**结论 / 修改内容 / 验证结果 / 风险与待确认项**。接口注明 Mock/真实，视觉注明实际视口与状态。
- “完成”只描述已验证范围；未执行、失败或环境不具备的检查必须明确说明。

## 7. 治理文件变更

- 只有用户明确要求时才能修改规则或决策；先检查职责、引用、重复、冲突和过时事实，再做最小一致改动。
- 新规则必须说明解决的风险、适用范围、验证方式和例外条件；口号、供应商瞬时事实和重复实现不得进入公共规则。
