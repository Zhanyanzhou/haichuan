# 海川珠宝 — AI 统一执行流程（WORKFLOW）

> 本文件只定义执行、验证与交付；权限与审批见 `AGENTS.md`，技术不变量见 `PROJECT_RULES.md`。
> 最近核对：2026-09-08（压缩重复规则，保留分层验证与治理门禁）。

---

## 0. 决策顺序

1. 按 `AGENTS.md` 核对安全与授权。
2. 明确用户结果、可观察验收和不改范围。
3. 实际行为以运行结果、当前代码、合同、Schema 和配置为证；预期行为以现行需求、合同和技术规则为准。两者冲突时核对版本、环境和数据来源。
4. 产品与品牌只认 `docs/PROJECT_GUARDRAILS.md`，不得被技术便利或专项文档覆盖。
5. 以已确定的业务闭环和目标验收层级组织实现，完成必要上下游接线；在满足完整结果的前提下控制维护成本，不以局部改动最小化截断流程。

## 1. 最小启动

- 实施任务读取本文件；`PROJECT_RULES.md` 与 `docs/` 只读相关章节，不循环加载全部治理文档。
- 仅在任务依赖工作树或准备修改时查看简洁状态和目标 diff；现有改动不覆盖、回退或混入。
- 复杂任务在内部明确目标、验收、影响、风险和阶段；目标清楚且已获授权时直接推进，不要求重复确认。
- 取证只做能增加信息或排除假设的最小检查；大合同、生成文件、日志、diff 和浏览器树只保留目标片段、断言或摘要。

## 2. 直接执行与暂停

用户已明确要求结果，且范围内方案不改变重大产品取舍、操作可逆、未命中审批项，或相关审批项已被当前有效请求精确授权时，直接执行；不得重复索取已有授权。

仅在以下情况暂停受影响部分：缺失信息会显著改变结果；需要未获授权的新权限、高风险操作或外部协调；范围实质扩大；现有改动冲突；权威来源经核验仍无法收敛。普通命名、局部布局和实现细节采用最小合理假设；未受影响工作继续推进。

实现任务以“用户结果闭环、必要调用链和失败路径无已知缺口、取得相称的当前证据”为终止条件。不得把闭环必需项留成后续建议；阶段只能拆分可独立验收的结果。

### 2.1 业务闭环与上线准备

适用于用户要求业务闭环、系统联调或上线准备的任务，防止局部功能通过检查后，必要上下游仍有缺口。只读检查、局部修复和文档任务仍按本次范围执行，不自动扩展为整站上线。

- **确定首发范围**：以当前用户要求和 `docs/PROJECT_GUARDRAILS.md` 为准，沿用 `docs/acceptance/feature-completion-matrix.md` 与 `docs/acceptance/end-to-end-flow-matrix.md` 明确必须交付的用户旅程、角色、起止状态、目标环境及成功和失败验收；未决业务或品牌取舍集中询问，不自行增加或删减首发能力。
- **追踪并解决缺口**：沿每条旅程检查实际涉及的前台、后台、接口、持久化、消费端和运行环境。验收证据写入既有验收矩阵，执行依赖与下一动作沿用 `docs/AI_PROGRAM_LEDGER.md`，不另建平行台账。缺口记录验收条件、当前版本与环境、已有证据、阻断原因及解决动作；属于已授权目标的普通实现持续完成，真实受阻部分明确解除条件，其余工作继续推进。
- **取得真实闭环证据**：按第 5 节验证目标层级，覆盖真实接口、保存后重新读取、相关角色与消费端结果，以及失败反馈和恢复路径。Mock、类型检查和构建只证明各自范围；目标要求真实联调或上线而证据不足时，不得以本地完成结案。
- **形成上线候选**：按 `docs/PRODUCTION_RELEASE_RUNBOOK.md` 和当前配置核对候选版本、必要工程门禁、目标环境、域名/TLS、配置与凭据就绪状态、适用的数据迁移和备份恢复、监控及回退方案；记录每项证据和未满足条件，不读取或记录秘密值。沿用 `deploy-checklist` 核对部署门禁。所需高风险操作在方案和可达验证准备好后按 `AGENTS.md` 核对授权，再执行获批动作；上线后以目标环境的真实用户旅程和持续运行证据验收。
- **按阶段交付**：区分已验证闭环、上线阻断、待授权或环境受阻项，以及已明确不在首发范围的事项。准备就绪与实际上线分别报告；范围内必需项未闭环时明确剩余工作，不将其降为可选建议。

## 3. 按任务路由

- 产品定位、品牌、公开信息架构或页面职责 → `docs/PROJECT_GUARDRAILS.md`。
- 客户前台视觉、交互、响应式 → 先核页面目标，再按 `docs/UI_GUIDE.md` 实现，并验收真实桌面与移动页面。
- 管理后台体验 → `docs/UI_GUIDE.md` 附录 A 与 `docs/ADMIN_COPY_GUIDE.md`，按受影响页面、视口和状态验收。
- 模板设计 → 先读唯一标准 `docs/page-builder/template-creation-rules.md`，再按需读从属编辑与页面交接细则 `docs/page-builder/template-design-framework.md`、`PROJECT_RULES.md` §7 和当前机器合同；旧计划、归档、Skill 与现有代码不能恢复过期产品要求。页面装修消费继续按交接细则分别核对 Template Repository 与 `PageDocument`。属性面板验证覆盖所需合同、适配、保存、回显和公开渲染，不改造无关模块。
- 易变状态 → 先查 `docs/CURRENT_STATE.md`，再以当前代码、配置和运行结果复核；状态文档不授予权限。
- 接口、数据、权限或生产风险 → 读取相关规则和调用链，按第 2 节核对授权。
- 解释、审计或状态报告 → 只读取回答所需证据。

专项文档定义项目事实，Skill 定义工作方法。用户点名的可用 Skill 必须使用；否则只在其描述真实匹配时触发。普通小改动不为形式追加法庭审校、完整计划、多任务或全仓验证。

旧计划和旧对话中的优先级只约束原任务。用户更新目标后，以现行规则重排，但不得绕过安全、权限、单一事实源和发布门禁。产品或品牌方向获批变化时，先更新 `docs/PROJECT_GUARDRAILS.md`，再同步受影响文档。

## 4. 实施边界

- 只改授权目标，优先修根因和共享入口。验证发现的旧问题，仅在与当前子系统相关、原因明确、低风险、范围小且可验证时顺带修复；不得通过删除测试、跳过检查或降低规则制造绿色结果。
- 每轮检查必须增加证据或排除假设；连续两轮没有新信息时停止重复扫描并报告最有价值的下一验证。
- 权限、范围或现有改动冲突时只停止受影响写入；其余工作和只读取证可以继续。

## 5. 风险分级验证

先确定准备声明的层级，再选择证据。单项验收只证明明确范围；整站目标以 `docs/PROJECT_GUARDRAILS.md` 第 2 节和 `docs/acceptance/feature-completion-matrix.md` 为准。

| 完成层级 | 可以声明 | 不能替代 |
| --- | --- | --- |
| 本地实现 | 当前代码、合同和目标检查证明范围内闭环 | 真实 API、数据库、权限、发布或生产 |
| 真实联调 | 指定非生产环境的真实 API、持久化、权限和重载已验证 | 发布版本、CDN、生产权限和回滚 |
| 上线 | 获批环境的发布、公开路由、缓存、监控和回退已验证 | 不得由本地、Mock 或单次构建推断 |

高层环境未请求、未授权或不可达，不否定已验证的低层结果；但低层关键路径未验证时，只能声明“已修改，待对应验收”。未请求上线时写“上线不在本次范围”。

以下为相关且环境可达时的最低基线；项目脚本、CI 或风险要求更多检查时继续执行。

| 改动类型 | 最低验证 |
| --- | --- |
| 文档、规则、提示词 | 引用、重复、冲突、过时事实、正反场景、`git diff --check`；Skill 另查触发边界 |
| 前端逻辑或类型 | 相关类型检查、构建或目标测试，覆盖受影响状态 |
| 后端接口 | 后端构建或目标测试，覆盖成功、失败、权限和边界 |
| 前后端契约 | 两端检查与合同/目标测试；任务包含真实持久化或上线时再做真实联调 |
| 局部视觉或文案 | 受影响桌面/移动视口、内容和状态 |
| 组件布局 | 受影响断点、溢出、键盘、对比度和加载稳定性 |
| 全局导航、令牌或骨架 | 代表性视口与关键状态矩阵 |
| 单个模板或兼容适配器 | 对应合同、结构、内容状态、视口及编辑器/预览/公开端路径 |
| 共享模板结构或 Renderer | 节点/槽位、响应式、各消费端及受影响适配器；只有修改全目录行为时才全验兼容集合 |
| 属性面板或实例覆盖 | 控件状态、编辑、撤销/重置、可达环境的保存与回显、画布和只读 Renderer；按能力覆盖加载、空、错误、失效、无效引用与数量边界 |
| 数据库、交易、权限或生产 | 获批方案要求的成功、失败、并发、回退和环境门禁 |

验证结论不得超过证据：构建不证明视觉，Mock 不证明真实接口，本地不证明生产。无法执行的必要检查须说明原因；保留命令、退出码和关键失败摘要。

本地测试截图、Trace、HTML 报告、临时构建和缓存统一写入已忽略的 `test-results-*`、`playwright-report` 或 `.codex-tmp/`；结论写入正式验收文档后删除临时产物，不在工作区根目录或 `client/` 下按轮次堆叠目录。

触及共享装修模型、跨消费端 Renderer/Inspector、草稿发布生命周期、权限、数据、交易、发布、生产或用户指定关键里程碑时，需要未参与实现者只读复核；普通局部可逆工作由主任务验收。复核者不修改候选，缺陷交回写入者；环境不足时标为未完成，不得宣称门禁通过。

## 6. 交付

- 按实际层级给出结论、修改、验证、未验证项和剩余风险；接口注明 Mock/真实，视觉注明视口与状态。简单任务可合并为一段。
- “完成”只描述已验证层级。未请求的更高层验证标为“不在本次范围”，不得冒充已完成，也不得描述成本地实现缺口。

## 7. 治理文件变更

- 仅在用户明确要求时修改规则或决策；先查职责、引用、重复、冲突和过时事实，再做最小一致改动。
- 新规则应有明确风险、范围、验证和例外；不写口号、瞬时供应商事实或重复实现。
- 按影响做正反场景推演。授权与执行规则至少覆盖：只读检查直接进行；普通实现不重复请示；精确授权可执行；笼统请求不授权高风险动作；数据库、支付、生产和敏感信息仍受门禁；脏工作树重叠时暂停；本地 worktree 可用但网络共享仍拒绝。
