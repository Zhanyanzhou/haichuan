# 海川珠宝 — 项目技术硬规则（PROJECT_RULES）

> 本文件规定**现在必须遵守什么**（技术实现硬规则），不按模型或工具分别建立规则。
> 架构决策与【待决策】事项见 `docs/DECISIONS.md`；AI 执行流程见 `WORKFLOW.md`。
> 安全、工作区、敏感信息与审批见 `AGENTS.md`；项目与品牌边界见 `docs/PROJECT_GUARDRAILS.md`；共享工作区协作见 `docs/AI_COLLABORATION_STANDARD.md`。
> 最近核对：2026-09-03（§7 按“模板设计框架优先”重新收敛事实来源与长期不变量；§8 资金门禁拆为条目）。

---

## 0. 职责分工与优先级（不建第二套规则）

完整的规则职责索引只认 `docs/PROJECT_GUARDRAILS.md` 第 1 节；该文件是产品、品牌与公开网站方向的唯一基线，本文件不得重新定义方向。权限与行为冲突时先遵守 `AGENTS.md`，再核对用户本次明确授权；仍与项目基线、技术规则或已批准决策冲突时必须报告，不得自行挑选。`docs/UI_GUIDE.md` 只在全站视觉与体验范围内具有详细解释权，不能覆盖安全、事实真实性或业务授权；客户前台适用正文，管理后台适用附录 A。

实际行为与预期标准的取证、裁决和暂停统一按 `WORKFLOW.md` 第 0、2 节。技术不变量定义预期约束，当前运行只证明实际行为；不凭旧文档改写当前实现，也不以运行中的缺陷否定现行批准需求和合同。

---

## 1. 单一事实来源（禁止建第二套）

| 事实                | 唯一来源                                                                          |
| ------------------- | --------------------------------------------------------------------------------- |
| 数据库模型          | `server/prisma/schema.prisma`                                                     |
| 后台用户 / 前台客户 | `User` 模型 / `Customer` 模型（见 §2）                                            |
| 商品稳定业务标识    | `Product.code`（`@unique`）；`Product.id` 只作内部关联和旧链接兼容                 |
| 商品图选取          | `client/src/utils/productImage.ts`（listing→primary→首张 FRONT→首张→placeholder） |
| 统一响应包装        | 后端全局 `TransformInterceptor`（`{code,data,message,timestamp}`）                |
| 响应解包            | `client/src/utils/unwrap.ts`（`unwrapResponse`/`unwrapList`）                     |
| 前端 HTTP 传输      | 当前共享 HTTP 客户端及拦截器；精确位置以代码与 `docs/CURRENT_STATE.md` 为准        |
| 前台页面装修        | Puck `PageDocument` 体系（见 §7）                                                 |
| 配置加载            | `@nestjs/config` `ConfigModule.forRoot({ isGlobal: true })`                       |
| 材质码到中文        | `client/src/utils/material.ts`（`getMaterialLabel`）                              |

**禁止**为上述任一新建平行来源。单一 HTTP 客户端不等于所有领域 API 必须集中在一个文件；允许按业务域拆分服务模块并由兼容入口统一导出，只要共享同一传输、鉴权、错误和响应处理。

商品新生成的公开链接和 Puck/CMS 持久化引用应优先使用 `Product.code`。现有数字 ID 路由与历史数据可以双读兼容，未经迁移和回归验收不得强制改写或删除；公开 API 如返回 `id`，只能把它视为不透明关联键，不能依赖其业务含义或长期稳定性。

---

## 2. User / Customer 边界（硬规则）

- **User** = 后台员工；**Customer** = 前台客户。当前角色、字段和游客兼容状态属于易变化事实，以 Schema、类型和 `docs/CURRENT_STATE.md` 为准。
- 两者是**两套独立体系**，代码与文档中不得用"用户"模糊指代。
- 两域的令牌和守卫必须可靠区分身份类型，不得接受另一身份域的令牌；当前共享密钥时不得移除 payload `type` 校验。
- 匿名、客户和后台员工接口必须通过服务端显式表达访问边界；修改前从当前全局 Guard、装饰器和调用链复核实际行为，不凭旧文档推断。
- 后台受限能力必须在服务端声明角色或更细粒度权限；重复 Guard 是否保留由当前框架注册与可读性决定，不作为第二套权限来源。
- 权限只认 `@Roles` 角色白名单；**前端隐藏菜单/按钮 ≠ 服务端拒绝调用**。需要强制细粒度权限时必须同时在后端补守卫。
- 后台员工与前台客户的刷新会话必须独立存储、独立吊销并保持身份域标记；浏览器 Cookie 鉴权的写请求必须通过 CSRF 和精确 Origin 校验。迁移期兼容 Bearer 不得让两域令牌互认。

---

## 3. 后端分层（硬规则）

- **Controller** 只做路由、鉴权、参数接收、委托 Service、返回业务对象。
- **Controller 禁止**：注入 `PrismaService`；写业务规则；直接读写数据库；在 Controller 内做图片处理/状态机/价格校验。
- **Service** 承担业务规则、事务、Prisma 读写；数据库读写**统一通过**构造注入的 `PrismaService`（`@Global() PrismaModule` 提供）。
- 不以类型断言绕过 Prisma 类型作为新增功能的常规写法；遗留例外需局部、可说明并有迁移方向。

---

## 4. 输入验证（硬规则）

- 新增/修改写接口**必须**建立 DTO（`dto/*.dto.ts`）并用 `class-validator` 装饰器（`@IsString`/`@IsInt`/`@IsEnum`/`@IsNumber`/`@Min`/`@MaxLength`/`@ValidateNested` 等）。
- Controller **必须**用 `@Body() dto: XxxDto`；**禁止**新增接口用 `@Body() body: any`（会绕过全局 `ValidationPipe` 的 `whitelist:true`）。
- 全局 `ValidationPipe`：`whitelist:true, transform:true, enableImplicitConversion:true`（`main.ts`）。

---

## 5. 响应与异常（硬规则）

- 成功响应由全局 `TransformInterceptor` 自动包装；Controller 直接返回业务对象，**禁止**手写包装。
- 异常由全局 `HttpExceptionFilter` 统一（已映射 Prisma P2002/P2025/P2003/P2014）；**禁止** Controller 内 try/catch 后手写响应体。
- **禁止**在 Controller、日志、响应中输出密码、Token、客户隐私或完整第三方响应。

---

## 6. 前端实现（硬规则）

- 所有 HTTP 调用必须走共享 HTTP 客户端及领域服务；禁止新建第二个 axios 实例或在组件内直接 `axios.*`。领域服务允许分文件，并可由 `services/api.ts` 兼容导出。
- 响应解包必须走共享解包工具；禁止在页面组件里重复实现包装层解析。
- 前后端边界类型必须同步；当前类型生成方式和目录属于易变化事实，以代码与 `docs/CURRENT_STATE.md` 为准。
- 信任边界和业务数据不得使用未收窄的 `any`；确需兼容第三方或遗留数据时优先 `unknown` + 校验，例外必须局部且可解释。
- 全局状态沿用已批准的状态体系；具体 Store、开关与权限导出修改前以当前代码复核。
- 异步界面必须覆盖实际可能出现的 loading、empty、error 和 success 状态；同步静态组件不为满足模板机械添加空状态。危险操作必须有与风险相称的确认和反馈。
- 客户前台必须先符合 `docs/PROJECT_GUARDRAILS.md` 的项目方向，再按 `docs/UI_GUIDE.md` 落实视觉与体验；本文件不复制品牌方法或另建审美方向。
- 公开信息架构与品牌/选款页面职责只认 `docs/PROJECT_GUARDRAILS.md`；前端不得另建第二个搜索筛选中心。商品详情的 `SalesMode` 主行动、合作资格与报价通道边界按 `docs/DECISIONS.md` D.19 和 `docs/PUBLIC_ACCESS_MATRIX.md` 执行，不得把客户身份或专属价格写进 `SalesMode`。
- 视觉数值、色盘、字体、图片、组件、动效、响应式和无障碍细则只认 `docs/UI_GUIDE.md`；验证范围与改动分级只认 `WORKFLOW.md`。禁止在页面或其他规则文件中另建第二套品牌标准。
- 新增样式优先使用现有设计令牌、组件和 Tailwind 约定；不得以大量任意像素、临时颜色、内联常量或页面私有按钮体系绕过全局一致性。动态计算值可使用行内样式。
- 管理后台以效率、清晰、状态完整性、响应式和无障碍为第一目标；未经单独批准，不把客户前台的大留白、衬线标题或编辑型动效全局套入后台。

---

## 7. 店铺装修（硬规则）

本节只保留长期技术不变量。模板设计唯一产品标准是 `docs/page-builder/template-creation-rules.md`，编辑与页面交接按需读取其从属 `template-design-framework.md`；当前实现与迁移状态见 `docs/CURRENT_STATE.md`，不得反向充当产品要求；验证按 `WORKFLOW.md` 第 5 节执行。

- 前台页面装修唯一采用 Puck `PageDocument`，母模板唯一采用版本化 `TemplateDefinitionV2`；不得恢复旧装修体系、创建平行内容系统，或把“固定模板”“动态模板”“新版模板”发展为并列产品模型。
- 模板事实按职责分源：`template-definition.schema.json` 定义结构与能力边界，`DynamicTemplate` 草稿和不可变版本承载实际模板定义与目录状态，`content-templates.contract.json` 只负责当前活动的 `hero` 首屏测试样例及页面级规则，`PageDocument` 只保存页面实例内容与允许覆盖。Renderer、Inspector、目录、适配器和校验器都是消费者，不得自行建立第二份模板事实。
- 模板设计 V2 主流程通过七步配置形成 Template Recipe，选择即时更新临时画布预览，确认后由统一 Generator 生成完整母模板，再进入四区精修、保存与独立发布。用途、画布和基础风格是单一可修改设置，区域、图片和内容可重复添加；预设只决定初始值，不限制后续编辑或重新覆盖精修。创建选项、操作次数、数值与验收只认 `docs/page-builder/template-creation-rules.md`。模板目录管理 Repository 中的普通模板，未保存会话独立呈现，复制保持独立身份。代码内仅保留 `hero` 首屏测试样例；它不是普通动态模板目录项，不能替代 Repository 中已保存、已发布的模板。注册节点能力、合法嵌套、页面角色与固定业务区校验继续有效。
- 页面装修与模板设计建立在同一 Shared Editor Core 上，共享 Repository、Renderer 和通用编辑基础设施，但严格隔离会话、写入对象和发布结果。母模板是完整根构图；模板设计负责结构、槽位、样式、响应式和页面可编辑范围，页面装修负责当前实例的真实内容与受控覆盖，任何模式都不得注入任意 HTML/CSS。
- 页面装修与模板设计必须共用同一响应式四区外壳，不得按模式建立第二套布局、持久化个性宽度，或在模式/断点切换时丢失选择、输入和未保存状态。具体断点、覆盖/停靠方式、侧栏可用宽度和画布分配只认 `docs/UI_GUIDE.md` 的后台装修工作区规则；本文件不复制易漂移的视觉数值。
- 模板设计 V2 使用数据 Schema 3，槽位默认图文可编辑并随定义保存；Template Recipe 只记录初始生成来源，重新打开和保存不重新生成。系统中性示例与临时试排独立于持久化默认内容，不得静默变为页面或业务事实。Schema 1/2 按原版本兼容读取，升级与复制只产生明确的新草稿或新身份，不改写历史发布快照。具体数据与默认值边界见 `docs/page-builder/template-creation-rules.md`。
- 模板草稿、不可变模板版本与页面发布版本必须保持独立。页面实例锁定精确 `templateId + templateVersion`；模板发布不得自动修改页面，页面只有显式升级草稿并重新发布后才使用新版本。历史页面、版本和未保存草稿不得因规则收敛而改写或丢失。
- 旧固定模板的合同、Renderer、Inspector、适配器、规则和专用验收均已删除，不提供只读兼容或恢复入口。数据库中的既有页面、版本和模板记录不由代码清理任务自动删除；如果正文仍引用已删除类型，运行时不得伪装为可渲染内容。`hero` 仅作为显式测试样例消费当前合同，不自动保存、发布或写入页面。
- Puck 模板和运营配置必须遵守 `docs/PROJECT_GUARDRAILS.md` 与 `docs/UI_GUIDE.md`；可配置不等于可以绕过品牌、真实性、版权、性能、响应式、无障碍或发布门禁。
- 页面内容按语言维护发布状态：中文无前缀与英文 `/en` 共享同一 `PageDocument` 事实根，但每个语言版本独立审核、发布并记录发布版本指针与内容哈希。`EDITOR` 仅草稿、预览、提交；`ADMIN` 与 `SUPER_ADMIN` 才能发布。

---

## 8. 数据生命周期（硬规则）

- 删除、停用与归档语义必须由 Schema、Service 查询和接口契约一致表达，不能仅靠前端隐藏。
- 当前哪些模型使用 `deletedAt`、`isActive` 或其他状态属于实现事实，记录在 `docs/CURRENT_STATE.md`；修改前检查所有读写与关联查询。
- 不得把已知死字段或过渡实现固化为永久规则。需要统一软删除机制时按数据库与架构变更审批。
- 商品、报价、价格、库存和销售资格不得复制事实：`SalesMode` 是作品对外呈现的公开主行动模式（展示咨询、选款、预约、标准直购或定制报价），不等于排他的报价通道，也不承载客户身份、合作资格或客户专属价格。当前标准零售 `DIRECT_PURCHASE` 的成交价只认有效 `ProductSKU.price`，`Product.price` 只作派生最低价缓存；高级定制与合作蜡模使用各自独立、可审计的报价事实。可售库存只认 `Inventory`，遗留 `ProductSKU.stock` 不得参与预占、核销或可售判断。双客群与三报价业务决定只认 `docs/DECISIONS.md` D.19。
- 普通客户与合作商家使用同一作品/设计事实，不复制商品；公开零售报价、定制报价和合作蜡模报价必须按身份与交易通道隔离。未审核、待补充、被拒绝或暂停的客户不得读取合作价格或创建合作订单；前端隐藏不能代替服务端拒绝。
- 红蜡、紫蜡合作业务以确认的 3D 文件版本及其固定蜡模重量为结算依据；预计黄金重量约为蜡重十倍只作内部生产参考，不得替代订单结算重量或普通珠宝成品报价。有效客户专属双克价优先、系统默认价兜底，具体默认值与经营口径只认 `docs/DECISIONS.md` D.19；数据校验数值见 `docs/PRODUCT_DATA_CONTRACT.md`。
- 合作价格和附加费用必须可配置、可追溯：`SUPER_ADMIN` 与 `ADMIN` 可以维护客户专属红蜡/紫蜡克价、生效区间与原因；费用支持按批准能力启停并在确认前逐项展示。任何调价不得反向改写历史报价或订单。
- 发布资格与可购买资格必须分开：按 `SalesMode` 执行公开行动与发布门禁；当前零售购物车、零售结算和直接下单只允许 `DIRECT_PURCHASE`，其 0 库存商品可以保持发布并显示售罄，但不得进入上述零售路径。不得据此推导未来只有 `DIRECT_PURCHASE` 才能形成订单；高级定制和合作蜡模在客户本人确认有效报价后可按各自状态机转单。
- 库存、产能与材料必须按报价通道裁决：标准零售使用 `Inventory`，高级定制与合作蜡模使用各自获批的产能、材料及文件/报价门禁，不得用成品零售库存替代。报价确认与转单必须由服务端校验客户本人身份，在事务内幂等生成订单并冻结完整、不可变快照；后台员工不得代替客户确认，失败不得留下部分订单或重复订单。完整状态机尚未具备时，危险确认或转单入口必须安全拒绝，不能以员工代确认、分步写入或临时字段绕过。
- 不得用假库存、虚假稀缺、伪造原价或没有来源的材质、证书、工艺和产地描述制造奢侈感。
- 员工权限以服务端角色白名单为准：`WAREHOUSE` 不参与付款查看或审核；`CUSTOMER_SERVICE` 可处理售后；员工“删除”只允许 `SUPER_ADMIN` 执行禁用，不得物理删除。改变上述业务访问政策、扩大角色权限或授予敏感权限属于高风险变更，须按 `AGENTS.md` 先获批准；让实现恢复到既有政策、补齐服务端拒绝或修复越权缺陷属于已授权目标内的普通安全修复。
- 在真实资金能力单独获批、目标环境与商户配置核验、成功/失败/回调/退款/对账路径完成真实验收之前，客户和后台均不得发起外部支付网关收款；后台可以保留历史查看、非网关人工审核和售后。
- 页面隐藏或客户交易开关不能代替服务端全局资金门禁：所有创建新网关交易的服务端入口必须消费同一个安全默认关闭的门禁。
- 回调必须独立完成验签、金额校验与幂等核销；不能因暂停新交易而丢弃暂停前已创建交易的有效回调。
- 当前接线与验证状态只认代码和 `docs/CURRENT_STATE.md`。

---

## 9. Mock / 生产隔离（硬规则）

- Mock 必须有单一、显式且生产安全默认关闭的入口；当前变量名和实现位置以 `.env.example`、代码和 `docs/CURRENT_STATE.md` 为准。
- 每次涉及接口的任务**必须**说明当前用 Mock 还是真实接口；上线/验收前**必须**以 `VITE_USE_MOCK=false` 做真实接口联调。
- **禁止**用 Mock 返回掩盖真实接口失败；**禁止**把 Mock 持久化数据（`localStorage: haichuan.mock-*`）当生产数据；**禁止**在 `VITE_USE_MOCK=true` 下声称功能已验证。
- 开发模板画廊不得进入生产路由；生产构建不得启用 Mock、静态演示内容或默认分析写入。公开内容缺失时只能返回明确安全状态或已发布内容，不能用开发 seed 冒充生产发布。

---

## 10. 敏感信息与环境变量（硬规则）

- **不读、不输出、不记录、不提交** `.env` 真实值（只允许检查变量名/示例结构）。
- **真实 API Key / Token / 密码只能存在于服务端安全环境**；**禁止**进入 `client/`、浏览器代码、文档、日志、截图、提交记录。任何 `VITE_*` 值均视为公开信息，`VITE_MOCK_ADMIN_PASSWORD` 只能是本地假凭据，绝不能复用真实密码或进入生产构建。
- 新增环境变量必须同步补到 `.env.example`。是否需要额外审批由变量用途判断：普通非敏感开发配置可随已授权功能实施，密钥、第三方服务、生产行为或工具供应链配置按 `AGENTS.md` 审批。
- 环境变量名称、用途和是否必需只认 `.env.example`；本文件不复制完整清单，避免两处漂移。Docker/部署环境是否真实注入仍以当前编排配置为准，不能把“写进 `.env.example`”误当成“容器已经可用”。

---

## 11. 数据库变更（硬规则）

- 已授权功能所需、向后兼容且可回退的本地 `schema.prisma` 修改与新增 migration 源文件可直接实施；删除/重解释现有数据、不兼容约束、修改历史 migration，以及对共享、真实或生产数据库的执行、回填或批量变更，必须按 `AGENTS.md` 先获得精确批准。
- 改 schema 的安全顺序：编辑 schema → `npx prisma validate` → 在任务隔离的一次性或可丢弃开发库生成并检查新增 migration → `prisma generate` → 同步 DTO、合同与调用方。不得为了生成 migration 默认连接项目常用库、共享库或真实数据源。
- 服务端必须从 `server/` 目录启动（`process.cwd()` 解析 `uploads/`）。
- Schema 改动后最低自检 `npx prisma validate`；新增 migration 必须检查数据影响和回退路径。只有一次性或可丢弃本地开发库可在已授权实现中直接执行，其他目标数据库按上一条审批。
- migration 文件存在、Client 已生成、测试通过或旧记录曾显示“无 drift”，都不能证明当前目标数据库已迁移。执行前必须确认精确数据库目标、当前迁移状态、备份与回退方案，并取得针对该目标的明确批准；目标不明时只允许静态检查。
- 未绑定精确目标、备份、回退、维护窗口和明确生产批准时，AI 不得执行生产 migration；上述条件与平台权限全部成立后，可由 AI 按发布手册执行获批步骤并留证，不要求用户手动代跑普通命令。未经精确批准不得删除或批量改真实业务数据。

---

## 12. 范围控制（硬规则）

- **一次只处理当前任务**；范围外问题只记录到"风险与待确认项"，**不得顺手重构**。
- 现有架构能可靠满足需求时默认沿用；若它已造成可验证的质量、性能或维护问题，先说明证据、边界和迁移方式，再按授权级别处理。
- **禁止**为同一功能建立第二套实现。
- 高风险行为按 `AGENTS.md` 处理；风险由业务影响、数据、权限、可逆性和外部契约决定，不由文件数量决定。
- 只有改变权威契约、公开行为、运行方式或当前状态时才同步对应权威文档；不为每个内部重命名制造文档噪音。文档与代码不一致时报告并按事实优先级核验。
- **不自行修改项目规则或架构决策**（见 `WORKFLOW.md`）。

---

## 13. 修改后必须验证（硬规则）

测试框架、脚本名称和覆盖范围属于易变化事实，必须从当前 `package.json`、`scripts/` 与 `client/tests/` 选择对应验证，不能因旧文档写过“无测试”或“已有测试”就推断当前覆盖。关键域（交易、页面构建器、选款咨询）改动后必须运行现有对应契约或端到端测试；缺失时明确报告覆盖缺口。

最低验证类型与标准见 `WORKFLOW.md`（单一执行来源）：

- 视觉改动**构建成功 ≠ 完成**，必须在实际页面验收；截图可以作为视觉证据，但不能代替交互、响应式、字体加载和状态检查。
- 接口改动必须同步核对共享 HTTP 客户端、受影响领域服务与调用方、后端 Controller/Service/DTO；如果通过兼容入口导出，再核对 `services/api.ts`。
- 未通过必要验证只能写"已完成代码修改，待验收"；**不得谎报验证通过**。
