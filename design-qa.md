# 属性面板图二落地视觉 QA

## 比对对象

- 参考图：`C:\Users\Administrator\.codex\generated_images\01a043d0-b237-7370-8469-7faeb9405747\exec-f4244dac-31ba-4cf3-8e19-0ceda1386eab.png`
- 参考图尺寸：1697 × 927；左侧为内容编辑，右侧为模板编辑。
- 实现截图（内容编辑）：`G:\网站搭建2\client\test-results\template-internal-editor-完-cc451-er-小画布只读导航，主画布编辑内容并实时同步同类设计-admin-chromium\double-poster-inspector-content.png`
- 实现截图（模板编辑）：`G:\网站搭建2\client\test-results\template-internal-editor-完-cc451-er-小画布只读导航，主画布编辑内容并实时同步同类设计-admin-chromium\double-poster-inspector-design.png`
- 实现截图尺寸：各 439 × 896；Playwright `admin-chromium`，页面视口 1600 × 1000，属性面板真实产品宽度。
- 状态：双图文模板，桌面端，选中主图；确定性自有 API 与图片夹具，不代表真实后端或生产素材。

## 全局结构比对

- 已保留参考图的核心层级：模块标题 → 内容/模板一级模式 → 固定对象轨道 → 模式专属可视工作区 → 当前对象控件。
- 内容编辑不显示位置、尺寸、层级和模板外观控件；媒体预览、替换、本页素材、图片链接、真实图片信息和替代文字保持首屏可见。
- 模板编辑以完整构图缩略导航开场，实际移动和缩放仍只发生在主画布；面板提供焦点、缩放、九点定位、X/Y/W/H 读数、精确位置与尺寸、层级和高级恢复。
- 参考图中的金色交互色未照搬；实现继续消费现有后台中性操作令牌，避免在属性面板建立第二套品牌色。

## 聚焦差异与修正记录

1. 原实现同时展示对象下拉、摘要卡、响应式状态、两套对象卡和大缩略画布，信息重复。已收敛为单一横向对象轨道与一条当前对象上下文。
2. 内容模式的小画布曾产生大块无效留白。已改为右侧紧凑“在模板中的位置”，与左侧操作提示并排。
3. 参考图包含比例、分辨率和适配状态，原实现只在异常时显示部分信息。已补充基于真实图片自然尺寸的常驻信息条；加载、异常和质量警告不伪造数值。
4. 双图文专属模板编辑原先缺少属性面板内的精确位置与尺寸。已补充九点定位、实时百分比读数和四个受合同边界约束的滑杆。
5. 参考图包含素材库入口。实现接入现有 PageDocument/本次会话素材集合，提供“本页素材”，未建立新的媒体事实源。
6. 顶部“已修改”长文本会挤压模板名。已改为状态点加设备名，并保留屏幕阅读器文本。

## 剩余非阻断差异

- QA 图片使用确定性几何夹具，不评价珠宝摄影品质；真实素材构图仍需在实际草稿中人工抽样。
- 参考图为概念对照板，单侧面板约为实际产品面板宽度的两倍；实现按当前 439px 产品宽度收敛了标签与按钮密度。
- 本轮没有改变全局品牌色、字体、依赖、HTTP API、数据库或 PageDocument 版本。

final result: passed

---

# 属性面板内部布局重构视觉 QA（2026-08-28，参考图 76d5ce3c）

## 视觉真值与实现证据

- 视觉真值：`C:\Users\Administrator\AppData\Local\Temp\codex-clipboard-76d5ce3c-698a-45a3-a9f7-dcc835281941.png`，851 × 1849 px。
- 实现顶部：`G:\网站搭建2\client\test-results\attribute-panel-visual-qa\implementation-template-layout-top.png`，439 × 996 px。
- 实现下部：`G:\网站搭建2\client\test-results\attribute-panel-visual-qa\implementation-template-layout-bottom.png`，439 × 996 px。
- 同屏对照：`G:\网站搭建2\client\test-results\attribute-panel-visual-qa\reference-vs-implementation.png`，1368 × 1040 px；参考图移除属性面板外框标题后按宽度归一化到 440 px，并与实现的顶部/下部滚动状态放在同一画布中检查。
- 视口与密度：Playwright `admin-chromium`，页面 CSS 视口 1440 × 1100，`deviceScaleFactor: 1`；实现属性面板逻辑宽度 440 px，元素截图因边界取整为 439 px。参考内部区域由 851 × 1763 px 等比归一化约为 440 × 912 px。
- 状态：首屏、桌面主图、模板编辑、确定性自有 API Mock；没有保存、发布或写入真实商家草稿。本证据证明浏览器渲染与本地交互，不代表真实后端或生产数据验收。

## 全视图与聚焦区域比较

- 全视图：对象上下文 → 当前任务 → 360 × 216 只读导航 → 比例与精确几何 → 层级/合同状态 → 能力型设置 → 本地草稿状态的层级成立；内容没有横向溢出或卡片套娃。
- 聚焦区域：同屏对照的中列保留首屏任务、导航和首要几何控件，右列保留层级、画框限制、能力型外观设置、恢复入口和状态栏，重要文字和控件均可读，因此没有再制作会丢失上下文的局部裁切。
- 参考图使用珠宝摄影作为主视觉预览；实现按照本轮批准方案使用合同几何示意，明确表达模板结构与选中边界。它不是媒体素材替代，也不承担上传、焦点或图片编辑职责。

## 五项视觉检查

- 字体与排版：沿用现有后台中文字体栈；任务标题、字段标签、辅助文案、按钮和状态的字号/字重层级与参考图同级，没有截断或异常换行。实现未引入第二套字体。
- 间距与布局：面板宽度、360 × 216 导航比例、16px 左右内容边距、任务卡与控件节奏一致；数值输入、三段定位和层级控制在 440px 内稳定排列。320px 与 200% 等效缩放由专项测试证明无横向溢出。
- 颜色与令牌：继续使用项目中性白、浅灰、石墨文字和现有绿色成功状态；边框、圆角和选中态克制，没有新增品牌色、渐变或重阴影。
- 图片质量与资产：模板导航使用清晰的合同结构示意，不伪造珠宝素材、不把参考图写入项目，也不使用低质量占位摄影。媒体内容态仍由真实素材链路负责预览与构图。
- 文案与内容：任务文案统一为“在主画布拖拽或缩放，在面板中精确输入”；对象名称、真实比例预设、区域宽高、画框限制、本地草稿/预览状态均来自当前能力或合同语义，没有伪造联动与公开一致性声明。

## 行为、响应式与无障碍证据

- 导航点击只改变选中对象；数值、对齐、层级、恢复、撤销/重做和桌面/移动独立覆盖均由 `admin-chromium` 专项测试通过。
- 位置按钮具备可理解名称和 `aria-pressed`，宽高为键盘可操作数值控件，层级当前值使用语义输出；模板导航为 listbox/option，只读且可选择。
- 440px 面板导航实测 360 × 216；1280 × 720、1600 × 1000、320px Inspector 与 200% 等效缩放均无横向溢出。
- 本轮截图用例未出现 React `Maximum update depth exceeded`；Mock 边界未启动真实 3000 端口，Vite 代理拒绝日志不作为浏览器功能通过或真实 API 证据。

## 比较历史与差异分类

1. 本轮正式同屏比较即为 `reference-vs-implementation.png`。未发现需要修复的 P0、P1 或 P2，因此没有在视觉比较后修改界面再伪造第二轮通过。
2. 导航由摄影预览改为结构示意、比例选项改为合同真实值、宽高改为百分比数值、溢出改为“限制在画框内 · 模板固定”，均为计划已批准的产品约束修正，不属于设计漂移。
3. P3：实现为了保留媒体对象真实的填充、缩放、圆角和阴影能力，需要比参考图多一个下部滚动状态；这些入口按能力出现，并没有重复层级或无内容折叠项。

未发现待处理的 P0、P1 或 P2。当前实现达到参考图的布局层级、密度和视觉克制度，同时落实了合同语义与交互修正。

final result: passed

---

# 属性面板参考图修复 QA（2026-08-28，1280 复核）

## 证据与归一化

- 视觉真值：`C:\Users\Administrator\.codex\generated_images\01a04631-d952-7281-9b7b-87d642ceb860\exec-82878537-72bf-49f9-8eae-c9f64b4a7649.png`，793 × 1983 px。
- 浏览器实现：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\reference-fix-15-compact-final.png`，1280 × 1160 px，CSS 视口 1280 × 1160，devicePixelRatio 1。
- 属性面板裁切：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\reference-fix-16-panel-final.png`，440 × 1100 px。
- 同尺寸同屏对照：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\reference-fix-17-side-by-side-final.png`，参考图按宽度 440 px 等比归一化为 440 × 1100 px；实现保持 440 × 1100 px，未比较浏览器外框。
- 状态：本地开发地址、Mock 模式、首屏桌面主图、内容编辑、真实空素材草稿；未保存、发布或注入参考图珠宝素材。
- 聚焦区域：同屏图中的对象上下文、任务条、媒体区、焦点、替代文本、点击行为、可见性与设置入口均可读，因此不再制作会丢失上下文的二次局部裁切。

## 比较历史

1. 初始实现证据：`reference-gap-audit-01-current-media.png` / `reference-gap-audit-02-side-by-side.png`。P1：1280 视口仅 360px 面板、默认停在模块级并堆叠全部字段、媒体空态使用旧上传卡、焦点与替代文本顺序错误、参考图必要操作缺失。
2. 第一轮修复证据：`reference-fix-11-side-by-side.png`。已修正 440px 面板、默认媒体对象、媒体任务组件、焦点坐标、三行替代文本、点击/可见性/四个设置入口；P2：垂直节奏仍偏松，1100px 高度下后续设置过多落到折叠区外。
3. 最终修复证据：`reference-fix-17-side-by-side-final.png`。压缩对象上下文、任务条、媒体操作、替代文本和设置行；在不缩小字体与点击目标的前提下，将滚动溢出降到约 50px，常用任务与首个后续设置在同一视口可见。

## 五项视觉检查

- 字体与排版：沿用后台现有中文字体栈；主任务 14px/600，字段与操作 12–13px，层级、行高、截断和替代文本计数可读；未建立第二套字体。
- 间距与布局：桌面面板固定 440px；内容态无小画布；任务按单列连续流排列；媒体框、双操作按钮、焦点、文本、交互和后续入口与参考图顺序一致。窄于 440px 的浏览器仍受可用视口约束，不产生横向溢出。
- 颜色与令牌：继续使用项目中性白、`#F4F5F5`、`#181A1B`、`#5F6568`、`#DDE1E2`，成功开关使用现有绿色；无新的品牌色或阴影体系。
- 图片质量与资产：参考图有珠宝摄影，当前 Mock 草稿无正式素材。实现保留真实 16:9 上传空态及 3360 × 1890 建议，不把参考图或构建资产写进草稿；上传后继续使用真实预览、自然尺寸与裁切链路。
- 文案与内容：对象路径、单一当前任务、尺寸建议、替代文本、点击行为、可见性、草稿与公开预览状态均为商家运营语言。替代文本上限保留当前 Schema 的 80 字；点击去向由真实行动入口控制；必需首屏主图显示“显示 · 固定”，未伪造无效能力。

## 交互与工程验证

- 内容/模板模式在全局工具栏往返切换成功；内容态小画布数量为 0。
- 模板态只读导航实测为 360 × 216；选择仍由主画布完成。
- 模块切换后自动选中当前端首个媒体对象，且不再把任务条自动滚出首屏。
- 浏览器控制台 error 为 0。
- `npm run build` 通过（TypeScript `--noEmit` + Vite production build）；仅保留既有大 chunk 警告。
- `git diff --check` 无空白错误；仅提示工作区 LF/CRLF 转换预告。

## 预期差异

- 摄影内容差异是 Mock 空素材状态，不是布局缺失；真实素材上传后需要另做裁切抽样。
- 参考图的“可见性”是可调开关；当前首屏主图按模板合同为固定必需对象，因此明确显示为固定，而不是提供无效开关。
- 参考图点击行为为“无”；当前模块已有唯一行动入口，属性面板提供真实跳转入口以避免建立第二套链接事实源。

未发现仍需修复的 P0、P1 或 P2。剩余差异均为真实数据状态或已明确的产品合同约束。

final result: passed

---

# 属性面板终稿视觉 QA（2026-08-28）

## 比对范围与证据

- 参考标准：
  - 内容媒体：`C:\Users\Administrator\.codex\generated_images\01a04631-d952-7281-9b7b-87d642ceb860\exec-82878537-72bf-49f9-8eae-c9f64b4a7649.png`
  - 模板布局：`C:\Users\Administrator\.codex\generated_images\01a04631-d952-7281-9b7b-87d642ceb860\exec-0a1cb4b1-0016-4cbc-a679-d8d22a5d2239.png`
  - 标题与行动：`C:\Users\Administrator\.codex\generated_images\01a04631-d952-7281-9b7b-87d642ceb860\exec-3f264680-0944-4a53-97b0-0281f570f0fe.png`
  - 发布阻断：`C:\Users\Administrator\.codex\generated_images\01a04631-d952-7281-9b7b-87d642ceb860\exec-e5a91ee1-0d23-481e-bd06-8a1ed14ed9e8.png`
- 实现证据：
  - 内容媒体：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\property-panel-content-implemented.png`
  - 模板布局：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\property-panel-template-implemented.png`
  - 标题与行动：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\property-panel-text-implemented.png`
  - 编辑器全景：`C:\Users\Administrator\.codex\visualizations\2026\08\28\01a04631-d952-7281-9b7b-87d642ceb860\editor-property-panel-final.png`
  - 同屏对照：`property-panel-content-comparison.png`、`property-panel-template-comparison.png`、`property-panel-text-comparison.png`（与上述实现证据同目录）。
- 浏览器：用户选定的 Chrome 扩展会话；1600 × 1100 视口；Mock 模式；未执行保存或发布。
- 数据状态：参考图使用已填充的珠宝图片；实现证据保留项目当前空素材状态，没有伪造商品或素材数据。因此图片内容本身不做像素一致性结论，只比较框架、控件与状态表达。

## 结构与交互核对

- 编辑工作模式只在全局工具栏出现 1 次；属性面板内 `tablist` 数量为 0，不再形成“导航套导航”。
- 桌面端/移动端切换只保留在全局工具栏；属性面板不重复设备导航或设备标题。
- 内容编辑状态不渲染模板小画布；图片、替代文字、焦点、文字和行动按当前任务连续呈现。
- 模板编辑状态只渲染 1 个固定 `360 × 216` 只读导航，内部按模板比例适配；对象选择有效，实际位置与尺寸调整仍在主画布。
- 属性面板底部只显示草稿同步或发布阻断状态；保存与发布保持为全局操作。
- Chrome 实测完成内容/模板往返切换；全局模式入口在工具栏重渲染后仍保持挂载；控制台 error 为 0。

## 视觉检查

- 层级：属性面板标题 → 当前对象 →（仅模板态）只读导航 → 单一任务提示 → 当前任务字段 → 状态底栏，与参考标准一致。
- 排版：14px 主信息、13px 辅助信息、36px 左右控件高度；未引入第二套品牌字体。
- 间距：采用 4/8 网格；面板分区使用 16–20px 内边距；媒体链接与替代文字之间的重叠已在第二轮修正。
- 颜色：白、`#F4F5F5`、`#181A1B`、`#5F6568`、`#DDE1E2` 的中性系统；未复制概念图的金色装饰色，也未修改全局品牌令牌。
- 圆角与边框：小圆角、单像素中性边框；无多层卡片套娃。
- 文案：任务提示以商家结果为中心；“标题与行动”同时呈现关联文字和唯一主行动，不再因点中单个文本节点而只显示一个字段。

## 修正记录与剩余差异

1. 第一轮发现空素材状态下“粘贴图片链接”与“图片替代文字”垂直重叠；已改为稳定网格并复核间距。
2. 第一轮发现文字对象只展示一个标题字段，不能完成参考图中的完整运营任务；已将标题、副标题、眉题、按钮文字与目标去向收拢为同一任务。
3. 最终加载发现 portal 挂载点会被工具栏重渲染清空；已增加挂载点替换/清空监听，并在 Chrome 中验证全局模式入口往返切换后仍为 1 个。
4. P3 测试缺口：当前 Mock 草稿没有发布阻断项，因此发布阻断态按组件结构和样式代码审查，未通过点击发布制造状态；这样避免改写受保护草稿。
5. P3 数据差异：参考图为已填充摄影素材，实现证据为空素材真实状态；需要真实商家素材后再做摄影裁切抽样，不构成本轮布局阻断。

本轮未发现待处理的 P0、P1 或 P2 视觉问题。

final result: passed

---

# 当前有效 QA 结论（2026-08-28）

- 当前有效的完整复核为上文“属性面板内部布局重构视觉 QA（2026-08-28，参考图 76d5ce3c）”，它取代旧通用字段结构和旧概念图的结论。
- 视觉真值：`C:\Users\Administrator\AppData\Local\Temp\codex-clipboard-76d5ce3c-698a-45a3-a9f7-dcc835281941.png`。
- 最终同屏证据：`G:\网站搭建2\client\test-results\attribute-panel-visual-qa\reference-vs-implementation.png`。
- 视口与密度：1440 × 1100 CSS px，devicePixelRatio 1；参考内部区域归一化为 440px 宽，实现属性面板为 439px 元素截图（440px 逻辑宽度）。
- 最终状态：确定性 Mock、模板编辑、首屏桌面主图；导航实测 360 × 216；86 项完整专项用例通过；没有待处理的 P0/P1/P2。

final result: passed
