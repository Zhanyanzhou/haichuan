# 模板设计唯一标准：步骤创建、预设生成与自由精调

> 生效：2026-09-11，按用户最新要求统一模板设计标准。本文定义产品与验收要求；各阶段实现及验证状态另见 CURRENT_STATE，不以规则生效代替功能验收。
> 本文是模板设计唯一的产品执行标准，覆盖创建、预设、生成、精调、管理、默认内容、版本边界与验收。适用于新建功能、后续改造和验收；不自动迁移或改写既有模板、页面和未保存草稿。

## 0. 唯一标准与文档分工

所有模板设计工作先读本文。下面的补充材料只在其职责内展开本标准，不得建立另一套模板创建流程、预设值、内容编辑限制或验收门槛。本文未逐项展开的有效编辑能力继续保留，不等于只能实现七步表单。

| 材料 | 与本标准的关系 |
| --- | --- |
| [模板编辑与页面交接细则](template-design-framework.md) | 从属细则：结构操作、属性事务、响应式、四区及页面交接；不得反向恢复空白创建或覆盖本文的操作次数与生成参数 |
| `template-definition.schema.json`、Template Repository、Renderer、`PageDocument` | 机器合同与实现证据，分别承载结构、版本、渲染和页面实例；当前代码不符合本标准时记为差距，不能据此修改产品标准 |
| [PROJECT_RULES](../../PROJECT_RULES.md) §7 | 技术边界；模板产品行为与数值引用本文，不另设产品流程 |
| [UI_GUIDE](../UI_GUIDE.md)、[ADMIN_COPY_GUIDE](../ADMIN_COPY_GUIDE.md) | 全局视觉、可访问性与文案要求；本文预设数值是单个模板的可编辑初值，不是全站令牌或后台外壳改版 |
| [CURRENT_STATE](../CURRENT_STATE.md)、验收矩阵、实施记录 | 只报告按本标准的已验证范围与差距；旧测试、数量、PASS 和在建源码不构成新标准通过证明 |
| 归档、临时产物、历史对话和旧审计 | 只用于追溯事实，不是执行规则，不得恢复已废弃的模板要求 |

安全与权限继续遵守 [AGENTS](../../AGENTS.md)，品牌及页面职责遵守 [PROJECT_GUARDRAILS](../PROJECT_GUARDRAILS.md)，验证层级遵守 [WORKFLOW](../../WORKFLOW.md)。唯一模板标准不合并这些独立职责，也不自行授权代码、数据库、发布或真实数据操作。

旧的空白创建主流程、旧七步任务链、强制逐个拖入元素、默认内容一律只读、禁止普通模板复制、固定模板批次数量准入等要求不再执行。后续若有产品规则调整，先更新本文，再同步从属细则和目标验收；不能仅在计划、Skill 或实现说明中追加例外。

## 1. 产品原则与范围

创建阶段追求确定性，编辑阶段保留自由度。废弃“新建 → 空白画布 → 逐个拖入元素才形成模板”的默认创建流程。系统通过步骤和预设完成约 80% 的基础设计，用户在生成后通过中心画布和属性编辑完成其余调整；80%/20% 是职责目标，不是完成率、权限开关或测试通过比例。

完整产品流程分为三个阶段。原有七步全部属于第一阶段，不代表模板设计的全部功能；第二阶段提供持续可用的微调工具，第三阶段检查并保存当前成果。

| 阶段 | 用户要完成的事 | 系统提供什么 | 阶段结果 |
| --- | --- | --- | --- |
| 第一阶段：创建基础模板 | 按七步选择用途、尺寸、布局、图片、内容和视觉预设，确认生成 | 推荐、明确数值、实时预览、自动布局和生成校验 | 一份完整、可编辑、尚未保存的基础模板 |
| 第二阶段：按对象微调 | 调整不满意的部分，按需增加或减少元素 | 对象选择、常用快捷值、精确输入、画布直接操作、撤销与重做 | 保留原模板身份及已有内容的精调结果 |
| 第三阶段：检查并保存 | 查看不同设备、处理问题、保存并重开核对 | 可定位的问题提示、只读预览、保存状态和失败恢复 | 可继续编辑的已保存模板草稿；发布仍是独立动作 |

这三个阶段是工作分工，不增加三页强制向导。第二阶段没有固定操作顺序，所有工具可反复使用；可以检查后返回微调，也可以先保存合法草稿再继续编辑。第一阶段第七步的“确认生成”只结束创建阶段，不能显示为“模板制作全部完成”。

标准流程为：

```text
新建模板
  → 1 模板类型 → 2 画布尺寸 → 3 布局结构
  → 4 图片配置 → 5 内容配置 → 6 视觉样式 → 7 确认生成
  → 自动生成完整基础模板 → 进入现有模板编辑器
  → 第二阶段：选对象 → 快捷微调 / 属性精调 → 按需增删
  → 第三阶段：预览检查 → 修正或保存模板草稿 → 重开核对
```

- 七步承担创建决策，不能退化为一次展示几十个输入项的长表单；显示当前步骤、已选摘要、返回与继续动作。返回已完成步骤保留用户输入，修改后的依赖和摘要同步更新。
- 类型推荐与用户显式选择分开。切换类型只更新未被用户修改的推荐项；已修改项保留，可主动重新应用推荐，应用前说明会替换哪些值。
- “通用模板”“自定义”和“自由布局”仍走标准流程、生成已选内容并满足基础质量，不成为隐蔽的空白创建入口。
- 新建入口可从模板列表或响应式工具栏打开，统一进入同一流程。创建前的配置属于临时创建会话，不写现有模板、页面、目录或正式版本。
- 创建前妥善处理来源会话的未保存状态；取消向导、返回修改或生成失败保留来源草稿和输入。未经明确保存或丢弃，不能覆盖来源会话。保存失败或冲突时留在来源并允许恢复。
- 第七步点击“创建模板”才生成一个新身份的完整内存草稿，成功后显示“未命名模板 · 尚未保存”；不自动调用保存、发布或页面插入。重复点击不能生成多个身份；生成失败保留配方并定位可修正问题，不进入半成品编辑器。
- 模板名称及交付资料可在编辑阶段补齐，不增设第八步。合法的未完成草稿仍可主动保存，模板发布和页面使用继续按原有独立流程验收。

### 1.1 一次设置与重复添加

“只能用一次”应解释为“同一模板只有一个当前设置”，不是“一旦选择就永远不能修改”。用户不应靠记忆操作次数理解系统；界面通过“设置 / 添加 / 修改 / 替换”的动词区分结果。

| 步骤与功能 | 数量或作用范围 | 创建时怎么做 | 生成后去哪里调整 |
| --- | --- | --- | --- |
| 1 模板类型：设置用途 | 每个模板一个当前主类型 | 单选，可返回重选 | 模板设置；改类型只改推荐，不重排已有设计 |
| 2 画布尺寸：设置画幅 | 每个模板一个根画布 | 选尺寸或自定义，不重复添加画布 | 模板整体属性；设备预览不是新画布 |
| 3 布局结构：选择整体骨架和图片结构 | 首次一个整体方案，含图片数量、角色和排列 | 选整体构图、图片组合和排列；Logo 与自定义图片增删统一在此完成 | 结构区“添加布局区域/图片”可重复；已有组在属性中改排列，不偷偷替换整个模板 |
| 4 图片配置：逐张设置图片特点 | 只调整布局中已有图片，不改变数量和角色 | 选图片后设置形状、外框比例、填充和圆角 | 属性改当前图片；新增或复制从结构工具进入 |
| 5 内容配置：批量添加内容槽位 | 按勾选产生多个槽位；文字类型可重复使用 | 勾选内容、添加具名自定义文字 | “添加文字/按钮”可重复；同类多个槽位具有独立身份、名称和顺序 |
| 6 视觉样式：设置初始样式 | 一个模板级基础方案，各区域/元素可有局部值 | 选择预设并生成具体数值 | 根、区域或元素属性精调；改局部不影响无关对象，不全量重套配方 |
| 7 确认生成：建立编辑草稿 | 一份方案确认只产生一个新身份 | 核对结果并创建；防止重复点击 | 后续保存当前草稿，不再次点创建来保存修改 |

Logo 和画布背景按职责管理：创建阶段的 Logo 位置、图片角色和画布背景图片由布局步骤统一管理，图片步骤只改外观，内容步骤不再提供 Logo 勾选。全画布基础背景属于整体设置，需要多品牌 Logo 或区域背景时在创建后的编辑器显式添加具名槽位。自定义图片 1～6 是首次图片方案的数量边界，不偷偷扩张为整个编辑器永久只能有六张图；后续增补仍受合法结构、可读性和空间校验约束。

### 1.2 选择即呈现，确认后进入编辑

- 创建界面同时显示“当前步骤选项”和“方案画布预览”；尺寸卡片按实际比例呈现。布局步骤的整体构图、图片组合和排列均使用紧凑线框示例：线框读取实际生成定义的几何，图片角色可辨识，文字简化为中性短线，不把难以辨认的完整文字缩进卡片。右侧使用同一生成结果经 Renderer 呈现。选定 4:3 后，全部布局线框和右侧方案均使用 4:3 画幅；图片步骤的形状、比例、填充示例则表达单张图片特点，不冒充整张画幅。宽屏可并列，窄屏可切换选项/预览而不丢输入。
- 每次有效选择立即重新计算临时方案并更新同源画布预览：尺寸改变整体外框，布局改变区域及图片结构，图片改变单张外观，内容改变文字与按钮，样式改变数值。尚未选择用途时显示不带价格、优惠等业务推荐的中性示例，并明确尚未选择；选择用途后才应用对应推荐。预览标明当前尚未配置的部分，不把中途预览称为最终成品。
- 第 1～6 步更新的是创建会话中的方案预览，没有模板持久化写入，也不替换编辑器中原来的草稿。第 7 步才确认完整结果并进入编辑；最终生成使用同一份已验证配方，不能预览一种、创建另一种。
- 改动导致空间不足或无效输入时保留用户选择、显示原因及修改入口；旧预览若保留必须注明“尚未更新”，不能假装已成功应用。未满足必要配置时不允许确认生成。
- 候选卡片在当前尺寸与内容下无法生成时，就地说明原因；不得换用另一套虚拟布局、裁掉已选内容或调整画布比例制造可用示意。尚未选择任何图片与内容时显示真实空方案状态，不偷偷补入槽位。
- 进入编辑后，“添加”只新增对象，“修改”只改当前对象，“替换布局”须明确影响范围并保留可映射槽位。预览只读、节点编辑和模板保存分别有明确动作，不能一次点击同时完成三种职责。
- 生成后每次局部添加直接出现在画布、结构树同步选中、属性显示当前对象；一次有效添加或预设应用对应一次可撤销操作。作用范围不清楚时先选区域，不能默认把内容塞入第一个区域。

### 1.3 生成后的调整导航

编辑器按“问题 → 对象 → 属性”帮助用户找入口：改整张大小选模板整体；改图片数量用添加图片；改图文顺序选布局组；改某张图的大小/填充选图片；改字号/文字选对应文字；改同级间隔选父组；改四边留白选所属区域。结构树和画布保持同一选择，属性标题说明正在修改的对象和范围；找不到或被遮挡的元素可从结构树定位。无需回到创建向导重新生成整张模板才能解决局部问题。第二阶段的功能范围、统一操作方式和保存检查见第 9.2～9.4 节。

## 2. 步骤 1：模板类型

类型回答“主要用于什么”，只提供推荐，不构成准入限制、页面视觉角色、业务权限或固定模板身份。用户可以选择商品促销，同时采用宽屏、左右布局、三张图且不展示价格。

| 类型 | 适用或目的 | 推荐尺寸 | 推荐布局 | 推荐图片 | 推荐内容 |
| --- | --- | --- | --- | --- | --- |
| 商品促销 | 商品推广、折扣活动、产品广告 | 4:5 | 上图下文 | 单主图 | 主标题、副标题、价格、原价、CTA 按钮 |
| 新品发布 | 新品介绍 | 4:5 | 中央主体 | 主图 + Logo | 品牌名称、主标题、副标题、CTA 按钮 |
| 活动宣传 | 活动信息与海报 | 9:16 | 全幅背景 | 背景图 | 主标题、时间、地点、CTA 按钮 |
| 品牌宣传 | 品牌形象与口号 | 1:1 | 中央主体 | 品牌图片 + Logo | 品牌名称、品牌口号、描述 |
| 社交媒体 | 社交内容 | 1:1 | 中央主体 | 单图 | 主标题、描述 |
| 新闻资讯 | 新闻与资讯 | 4:5 | 上图下文 | 新闻主图 | 标签（分类）、主标题、描述（摘要）、日期 |
| 人物介绍 | 人物档案 | 4:5 | 左图右文或上图下文 | 人物照片 | 姓名、职位、简介 |
| 通用模板 | 通用用途 | 不作强推荐 | 用户选择 | 用户选择 | 用户选择 |
| 自定义 | 完全自行选择后续设置 | 用户选择 | 用户选择 | 用户选择 | 用户选择 |

“品牌口号”使用具名的自定义文字槽位；新闻标题、摘要、人物照片等语义名称映射到对应原生槽位，不再维护第二套字段模型。人物介绍同时呈现两种布局推荐，生成采用用户确认的一个布局。

这些是模板制作选项，不改变海川公开页面定位；商品促销、价格、联系信息或品牌色的可配置能力不自动授权真实交易、公开营销、真实资料或全站品牌变更。

## 3. 步骤 2：画布尺寸

| 分组 | 名称 | 比例 | 默认宽 × 高（px） |
| --- | --- | --- | --- |
| 常用 | 正方形 | 1:1 | 1080 × 1080 |
| 常用 | 竖版 | 4:5 | 1080 × 1350 |
| 常用 | 手机全屏 | 9:16 | 1080 × 1920 |
| 常用 | 宽屏 | 16:9 | 1920 × 1080 |
| 更多 | 标准竖版 | 3:4 | 1080 × 1440 |
| 更多 | 海报竖版 | 2:3 | 1200 × 1800 |
| 更多 | 标准横版 | 4:3 | 1200 × 900 |
| 更多 | 社交横图 | 1.91:1（约） | 1200 × 628 |
| 自定义 | 自定义尺寸 | 由输入计算 | 输入宽度、高度；单位默认 px |

- 自定义支持锁定比例；锁定时改变一边同步计算另一边，解除后独立编辑。非正数、非有限值或超出受支持范围时保留输入并就近报错，不能静默夹取为另一个尺寸。
- 画布尺寸是模板设计基准，预览缩放不是修改画布尺寸，也不承诺图片导出或印刷能力。桌面、平板、手机继续使用现有响应式规则；初始生成必须给出可用的缩放或重排结果，不能把设计像素直接当成窄屏溢出理由。
- 比例与实际宽高同时展示；社交横图保留指定的 1200 × 628，不因比例标记的舍入改变尺寸。
- 创建会话中返回修改尺寸，保留已选布局、内容及各图片的比例、形态、填充等独立设置，按新画布重新计算候选卡片和方案预览。设计画幅与手机响应式重排分别验收；选择 4:3 不表示手机页面必须始终保持 4:3。

## 4. 步骤 3：布局结构

| 布局 | 基础骨架 | 典型用途 |
| --- | --- | --- |
| 上图下文 | 上方图片区，下方文字与按钮 | 商品、新闻、人物、品牌展示 |
| 上文下图 | 上方标题与内容，下方主图 | 宣传、品牌、简约设计 |
| 左图右文 | 左主图，右文字内容 | 人物、产品、商务介绍 |
| 左文右图 | 左内容，右图片 | 图文介绍 |
| 中央主体 | 中央主图或核心内容，上下标题与辅助信息 | 新品、品牌、商品主视觉 |
| 全幅背景 | 背景铺满画布，文字作为明确前景叠加 | 活动、海报、品牌宣传 |
| 顶部标题 + 中间主图 + 底部信息 | 三段排列 | 商品、新品、资讯 |
| 卡片布局 | 画布中央主要内容卡片 | 报价、人物、产品介绍、信息展示 |
| 左右分栏 | 两个主要同级区域 | 对比、双产品、图文组合 |
| 自由布局 | 创建所选基础内容，提供无异常重叠的初始位置，不锁定后续结构 | 自定义构图 |

布局描述是生成意图，须通过现有合法原生节点实现；区域、布局组和槽位都可选择和编辑。无图片时不生成多余空图片区；无文字时不保留假文案区。多图、横竖切换、长内容等组合不能静默忽略已选内容；空间不足时说明原因并引导修改，不能通过裁掉节点或异常缩小文字伪装合格。全幅背景的前后景重叠是预期设计，其他元素仍不得异常遮挡。

布局步骤分为整体构图、图片组合、图片排列三个有明确职责的选择区。整体构图候选只替换主布局，保留当前尺寸、图片及独立外观、内容和样式；图片组合候选改变数量和角色，复用可映射的现有槽位与设置。线框使用实际生成定义的几何，选中后的右侧方案与创建结果应对应，不能维护一套与实际生成无关的漂亮图标。此要求只影响创建会话，不改写已保存模板或历史版本。

### 4.1 图片组合与排列

布局确定可替换的图片位置，不要求上传真实图片，不自动生成或选择商业素材。

| 图片方案 | 槽位 |
| --- | --- |
| 无图片 | 0 个 |
| 单主图 | 1 个主图 |
| 主图 + Logo | 主图、Logo 各 1 个 |
| 主图 + 1 张副图 | 1 主图 + 1 副图 |
| 主图 + 2 张副图 | 1 主图 + 2 副图 |
| 双图 | 2 个同级图片 |
| 三图 | 3 个同级图片 |
| 背景图 | 1 个画布背景图片 |
| 背景图 + 主图 | 背景、主图各 1 个 |
| 自定义 | 1～6 个图片槽位 |

- 图片排列明确提供横向和纵向。同级图片分别固定并排或上下排列；一主多副分别固定主图在左、副图区在右（副图纵排），或主图在上、副图区在下（副图横排）。新创建方案默认横向 `row`；历史配方省略该规则或采用 `auto` 时保持既有自动排布，不重新生成已保存定义。单张比例改变只在既定区域中适配，不自动翻转已选排列。
- Logo 开关、自定义槽位名称/角色、增删图片集中在本步骤；总数与实际节点一致，同一 Logo 只生成一次。切换组合或移出图片说明影响，本次会话可恢复对应设置；不得因返回步骤、修改尺寸或用途推荐覆盖用户已明确修改的图片设置。
- 活动宣传推荐的“背景图”是单图槽位的背景用途，可由全幅背景布局推荐并在摘要明确显示；品牌图片、新闻主图与人物照片使用对应语义名称。
- 全幅背景布局优先使用显式背景槽位；没有背景槽位时，首张非 Logo 图片承担全幅背景。图片步骤和确认摘要必须说明该用途及跟随画布的实际比例、无圆角外框；保留该图片原先的独立设置，切换回普通布局时恢复。其他图片与前景内容继续独立排布，不能把全幅布局变成普通上图下文。
## 5. 步骤 4：图片配置

逐张选择布局中已经存在的图片，并在方案预览标识当前对象；只提供图片特点，不再提供数量、组合、角色或 Logo 增删。需要改变图片结构时通过“修改布局”返回，保留其他设置。无图片时显示明确空状态和返回布局入口。

- 图片形状提供直角、圆角（默认 24 px）与圆形，并可调整适用的具体圆角值；填充独立提供填满裁切和完整显示，以同一示例素材展示区别。填充模式与形状分别保存，选择圆形后仍可决定填满或完整显示；圆形采用正方形外框并圆形裁切。
- 图片比例提供：1:1、4:5、3:4、16:9、适应区域（自由比例）。外框比例、形状、填充和取景不能混为一个值；背景槽位跟随画布，Logo 可单独调整，不强制与主图共用比例。
- 单张设置不覆盖其他图片；全幅背景禁用不适用的形状和比例并说明实际采用值，切回普通用途仍保留独立设置。用户显式图片圆角优先于后续整体风格圆角。
- 中性占位显示语义和合理比例，缺图不坍塌。形状、比例、填充示例直观表达各自的区别；数量、主次和排列示例统一留在布局步骤。候选和右侧预览仍使用同一配方校验，不能绕过空间或内容合法性检查。

## 6. 步骤 5：内容配置

采用复选项，类型推荐只负责初始勾选；用户可增加或取消，不能因为类型而锁定价格、CTA 或其他内容。

| 分组 | 可选内容 |
| --- | --- |
| 基础内容 | 主标题、副标题、描述、标签、品牌名称、日期、时间、地点、联系信息 |
| 商品内容 | 商品名称、商品卖点、价格、原价、折扣、优惠信息、CTA 按钮 |
| 人物内容 | 姓名、职位、简介、社交信息 |
| 自定义内容 | 添加具名的自定义文字槽位 |

Logo 由布局步骤统一管理，内容步骤只提供返回布局入口，不再增删图片。内容按实际能力映射到文字、按钮或已有合法槽位。增减字段在已选图文关系中重新分配空间，不改变图片数量或固定排列；放不下时说明原因并定位修改入口。生成保留稳定字段身份、语义名称与阅读顺序，结构树、画布选择和属性编辑能定位同一对象。中性只读示例可辅助判断文字层级，但不进入模板默认内容、页面实例或公开输出。Schema 3 的默认图文可在属性中编辑并随模板保存；默认内容、临时示例和页面真实内容的优先级与隔离统一见本文 9.1，不得将随机占位变成真实价格或经营事实。

### 6.1 初始文字层级

设画布短边为 `S = min(width, height)`。生成时将以下关系解析成明确可编辑的数值；不能把所有文字设置为同一字号。

| 层级 | 初始数值规则 | 字重 |
| --- | --- | --- |
| 主标题 | 约 S 的 5%～7% | 700 |
| 副标题 | 主标题的 45%～60% | 500～600 |
| 正文 | 主标题的 30%～40% | 400 |
| 辅助文字 | 正文的 75%～90% | 采用清晰的辅助层级 |
| 价格 | 强调级，接近或略高于主标题 | 与强调层级一致 |

为避免开发时各自猜值，标准初值取：主标题 `round(S × 0.06)`，副标题 `round(主标题 × 0.52)` / 字重 500，正文 `round(主标题 × 0.35)`，辅助文字 `round(正文 × 0.8)` / 字重 400，价格 `round(主标题 × 1.05)` / 字重 700。高端与极简可将标题系数设为 0.07，其余层级继续按同一关系计算。这是本次流程完善确定的起点，可在属性中修改；像素取整不能把文字变为 0，无法满足基本可读性的尺寸应提示调整画布或内容。长文本换行、行高、最大字数和溢出反馈仍须验收，不能靠隐藏正文满足边界。

## 7. 步骤 6：视觉样式

先选预设，生成后再精调。以下数值是模板创建初值，不能成为编辑器允许值的白名单，也不取代 UI_GUIDE 的品牌与可读性规则。

### 7.1 风格

| 风格 | 初始设计意图 |
| --- | --- |
| 简约 | 大量留白、弱装饰、清晰排版、中等圆角 |
| 商务 | 规整结构、明确对齐、较小圆角 |
| 高端 | 大留白、大图、低信息密度、大标题 |
| 活力 | 明显色块、较强层级、较大圆角 |
| 科技 | 优先深色、高对比、线性元素 |
| 温暖 | 柔和背景、中等圆角、宽松间距 |
| 极简 | 更少装饰元素、更宽留白、弱边框 |
| 自定义 | 自行设置配色、圆角、间距、边距与对齐 |

风格改变建议参数，不删用户已选内容、不增加真实素材、不覆盖用户显式指定的配色与数值。“温暖”不自动增加未经批准的品牌色，“科技”不强制深色，“高端/极简”不以少元素为由丢弃已选字段。确定性生成器必须把风格意图映射为明确参数，而非仅保存一个风格名称。

风格的初始组合统一如下，具体数值读取本节各预设表；用户显式选择优先。对齐始终读取布局建议或用户选择，不由风格强制重置。

| 风格 | 配色建议 | 通用圆角 | 间距 | 内容边距 |
| --- | --- | --- | --- | --- |
| 简约 | 浅色 | 中 | 标准 | 标准 |
| 商务 | 浅色 | 小 | 标准 | 标准 |
| 高端 | 浅色 | 无 | 超宽松 | 宽松 |
| 活力 | 品牌色（主色未选时保留浅色并提示选择） | 大 | 标准 | 标准 |
| 科技 | 深色 | 小 | 标准 | 标准 |
| 温暖 | 柔和浅色 | 中 | 宽松 | 标准 |
| 极简 | 浅色 | 无 | 超宽松 | 宽松 |
| 自定义 | 保留当前合法值并允许修改 | 保留当前值 | 保留当前值 | 保留当前值 |

### 7.2 配色

| 配色 | 初始规则 |
| --- | --- |
| 浅色 | 背景 #FFFFFF；主文字 #111111；辅助文字 #666666 |
| 柔和浅色 | 背景 #F7F7F7；文字继续满足对比度 |
| 深色 | 背景 #111111；主文字 #FFFFFF；辅助文字 #BDBDBD |
| 品牌色 | 用户选一个主色，系统计算背景、强调色和按钮色，并检查文字对比度 |
| 自定义 | 用户设置颜色，显示不满足可读性时的就近反馈 |

品牌色只作用于当前模板配方，不改全局品牌色或 Logo。派生颜色必须是确定性且可编辑的实际值，不能仅记录“自动”。图片背景上文字还须验证实际叠加区域，纯色对比通过不能替代背景图场景验收。

### 7.3 圆角、间距、边距和对齐

| 圆角预设 | 无 | 小 | 中 | 大 | 超大 |
| --- | --- | --- | --- | --- | --- |
| px | 0 | 8 | 16 | 24 | 32 |

| 间距预设 | 基础间距（px） | 区域间距（px） |
| --- | --- | --- |
| 紧凑 | 12 | 24 |
| 标准 | 16 | 32 |
| 宽松 | 24 | 48 |
| 超宽松 | 32 | 64 |

| 内容边距 | 相对于画布的比例 |
| --- | --- |
| 紧凑 | 约 4% |
| 标准 | 约 6% |
| 宽松 | 约 8% |

边距采用画布短边为统一计算基准：四边初值 `round(min(width, height) × 比例)`，摘要与属性展示同一解析值；不对所有尺寸写死同一像素值。后续可分别改四边，不被比例建议锁定。基础间距用于相关内容，区域间距用于主要分区，不能把两者混用。

对齐提供左对齐、居中、右对齐；上图下文与商务左右布局默认左对齐，中央主体默认居中。布局默认只作推荐。元素级显式图片形状/圆角优先于通用圆角；用户可以在生成后用属性修改任何适用值，如把中圆角 16 px 改为 13、27 或 0 px。

## 8. 步骤 7：确认生成

确认页显示所有会影响输出的已选配置和解析摘要，至少包括：类型、画布宽高与比例、整体布局、图片方案/数量/角色/排列/形状/比例/填充、全部内容及自定义槽位、风格、配色、圆角、间距、内容边距和对齐。不能只复述类型推荐而省略用户覆盖；生成参数必须与最终摘要一致。

例如：商品促销；1080 × 1350（4:5）；上图下文；1 张主图、1:1；主标题、副标题、价格、原价、CTA；简约、浅色、中圆角、标准间距。其他实际选择也应在摘要中显示。

提供“返回修改”和“创建模板”。修改返回相应步骤并保留其他输入；未确认时不创建模板。创建只进入未保存的编辑会话，不保存草稿或发布，示例文字不转为默认内容。非法尺寸、矛盾的图片配置、不能容纳内容等问题需在创建前定位；结构问题返回布局、单图特点问题返回图片，不能把用户送到已移走控件的步骤。生成过程中显示进度与重复点击保护，失败后保留配方，重试不产生重复身份。

## 9. 自动生成、编辑和模板管理

系统负责完成十二项工作：创建画布、创建布局区域、创建图片槽位、创建内容槽位、计算尺寸、计算位置、设置间距、设置基础字号、设置颜色、设置圆角、建立元素层级、完成基础对齐。首次结果必须已经可用，核心内容可编辑，不能要求用户补齐布局才能开始。

生成器消费创建配方并输出现有 `TemplateDefinitionV2` 原生结构；配方只负责初始生成，不成为与模板定义并行的运行时结构来源。用户后续修改不被类型推荐或配方重新覆盖。结构合同、Repository、共享 Renderer 和页面 `PageDocument` 的分工不变；完善产品规则不自动授权修改 Schema、保存 API 或历史记录。

| 编辑区域 | 生成后的职责 |
| --- | --- |
| 模板列表 | 切换、新建、复制、删除、重命名；明确草稿、已发布与回收站状态 |
| 模板结构 | 查看背景、主图、Logo、文字、价格与按钮等节点；选择、排序、显隐、锁定；保留合法结构增补能力 |
| 中心画布 | 查看、选择、移动、缩放、对齐、复制、删除、调整层级和微调布局 |
| 属性编辑 | 精确调整尺寸、位置、字体、字号、颜色、圆角、间距、对齐、透明度、图片比例/填充、最大字数、是否允许替换等适用参数 |

- 所有生成参数都能在适用对象的属性中修改，支持合理的非预设值；非法输入保留可修正状态，取消和撤销有明确结果。字体沿用项目许可与能力边界，不默认安装新字体或修改核心字体。
- 图片与核心文字的设计、槽位规则以及新版本允许的默认图文在模板属性中可改，页面实例真实内容按模板开放范围编辑。编辑自由度不绕过结构合法性、锁定状态、业务角色和发布门禁。
- 模板列表复制是本次新增的目标能力：对用户明确选定的普通模板及来源草稿/版本创建独立身份的未保存副本；说明来源，保护来源和当前未保存会话，不共享可变草稿，不自动保存或发布，也不复制页面实例的真实内容或恢复退役固定模板。
- 节点复制仍只作用于当前草稿并可撤销；模板删除与节点删除区分，目录删除需确认目标和影响并遵守 Repository 的引用、归档/回收站及恢复语义，不删除不可变历史或破坏已发布页面引用。
- 四区比例、窄屏抽屉、共享选择和原有编辑历史继续遵守主规格；创建向导是进入编辑器前的临时流程，不新增永久第五区。

### 9.1 方案、默认内容与版本兼容

- 产品 V2 使用数据 Schema 3，继续读取 Schema 1/2；统一 `TemplateDefinitionV2`、Repository、Renderer 和编辑会话，不另建画布系统或数据库表。产品名称与数据 Schema 版本是不同概念。
- `templateRecipe` 是版本化的初始生成输入，包含类型、画布、布局、图片、内容、样式和规则；不保存选择、预览缩放、临时试排或撤销历史。生成器接收经验证的配方，输出合法原生节点和槽位。
- 最终模板定义是精调、保存和渲染的事实。人工修改只改当前定义，打开、保存和切换设备不从初始配方重新生成；不提供静默重套方案覆盖精调的入口。
- 媒体和内容角色复用统一槽位，几何归节点，合法默认图文统一存入 `defaultContent`。Schema 3 默认文字、默认图片可通过属性和统一编辑命令修改、撤销、保存与重开；图片创建阶段无需上传真实素材。
- 系统中性示例、临时压力试排、模板默认内容和页面真实内容分开标识及保存。系统示例与临时试排不进入默认内容或页面；默认内容也不能冒充未经核验的经营事实。
- 页面实例显式值优先于模板默认值；缺省按锁定版本读取默认值，显式清空默认隐藏，只有明确选择回退规则才使用默认值。页面始终锁定精确的 `templateId + templateVersion`。
- 样式只作用于当前模板，沿用受控颜色、字体和结构化样式能力，不接受任意 CSS/HTML。价格、原价等是内容角色，不改变交易或金价规则；背景能力不授权修改全局品牌资产。
- 旧版本默认内容按原版本兼容读取，不批量迁移、不改写历史发布快照；未知的退役固定类型明确不可用，不能伪装已兼容。升级或复制须明确来源与新草稿/身份，保留既有正式版本和页面引用。
- 模板保存、模板发布和页面发布是独立动作；只有页面装修中的“发布页面”能推进客户前台读取的 `publishedRevisionId`。页面实例未明确上传当前可达图片时允许发布但公开 Renderer 隐藏整个模板；模板默认图只服务设计与后台预览，不能替代页面上传或触发公开展示。已有正式版本、页面和未保存草稿不因新规则被改写。验证需覆盖默认内容编辑/撤销/保存回读、旧 Schema、显式值/缺省/清空及精确版本，不能只验证新建成功。
- 页面可重复添加同一或不同的 `primary-stage` 首屏主舞台，也可复制当前主舞台或接受产生新增主舞台的版本升级；数量不是目录、编辑命令、预检、发布或公开渲染门禁。多个有图且已随页面发布的主舞台按根内容顺序完整展示；首个可见主舞台使用页面级标题和首图优先语义，后续主舞台使用内容级标题与普通加载优先级。

### 9.2 第二阶段：微调功能清单

生成基础模板后，微调是正式的核心能力，不是“以后再补的高级功能”。主要入口仍是模板结构、中心画布和属性编辑，不增加永久第五区，不要求用户重新完成七步。按当前对象呈现功能，而不是把下表全部控件同时堆在一个面板。

| 用户想调整什么 | 选择对象与主要入口 | 必须提供的微调功能 | 修改边界 |
| --- | --- | --- | --- |
| 整张模板的尺寸或背景 | 模板整体 → 整体属性 | 宽高、锁定比例、背景颜色/图片、整体留白；显示实际单位和尺寸 | 修改当前模板；不另建画布，不改全局品牌参数，不重新创建槽位；内部布局按有效约束响应 |
| 图文排列与疏密 | 布局区域/父组 → 排列与间距 | 上下/左右排列、适用分列比例、同级顺序、组内对齐、同级间距、四边内边距 | 显示会受影响的子项；只改所选组，保留原图文和字段身份；流式排列不伪装成自由坐标移动 |
| 图片看起来太大、被裁切或不合适 | 图片 → 图片与外框属性 | 选择/替换模板默认图片、外框宽高与比例、锁比、填满/完整显示、适用取景焦点、圆角/圆形、透明度 | 外框尺寸和图片取景分开调整；不裁改源文件，不批量替换其他图片；无图时外框与填充规则仍可修改 |
| 文案、字号或文字层级 | 文字 → 内容与文字属性 | 模板默认文字、字体、字号、字重、颜色、行高、字距、对齐、文本区域宽度与适用换行策略 | 先显示文案与常用排版；修改字号不偷偷修改文案或最大字数，不裁字来掩盖空间不足 |
| 按钮的大小与外观 | 按钮 → 内容与外观属性 | 默认按钮文案、宽高策略、内边距、字体、字号、文字色、背景色、圆角、文案对齐 | 按钮外框对齐和按钮内文案对齐分别生效；行动目标、页面字段要求沿用原交付边界，不夹带页面发布 |
| 元素数量、位置和遮挡 | 对象/所属区域 → 结构工具或画布操作栏 | 添加区域、图片、文字、按钮；复制、删除、排序、显隐、锁定；适用的位置、尺寸、对齐与层级调整 | 添加/复制产生独立对象；修改不增加数量；删除节点不等于删除模板；受保护对象说明原因与合法处理入口 |
| 手机与桌面效果不同 | 当前对象 → 设备切换及对应属性 | 查看桌面/平板/手机、调整本端适用属性、显示继承来源、恢复继承、只读比较 | 设备查看不改数据；结构增删与本端样式的影响分别说明，不把多端当成多个模板 |
| 页面使用者能改哪些内容 | 槽位 → 页面开放范围 | 字段名称、必填/可选、适用最大字数、是否允许替换/编辑、适用隐藏规则 | 模板设计者调整默认图文与设计；页面使用者按开放范围填写实例内容，不把两种权限混为一谈 |

图片、文字、按钮、区域均可分别重复调整；同类的多个对象互不覆盖。模板整体、局部父组、单个元素和多选对象的作用范围必须明确。多选仅提供共同且合法的属性，混合值不显示为 0；批量操作沿用同一校验和撤销规则。

### 9.3 微调仍采用“快捷值 + 精确输入 + 即时反馈”

1. **先选对象**：画布与结构树均可选择，选中状态同步；属性顶部显示名称、所属区域、当前设备及修改范围。未选中时显示选择提示与模板设置入口，不暗中替用户选择第一个对象。
2. **先显示常用项**：文字先显示默认文案、字号和颜色；图片先显示默认图片、填充与比例；布局组先显示排列、间距和留白。更多适用参数按对象分组展开，错误与已修改状态不能藏在折叠区而没有提示。
3. **快捷操作继续可用**：圆角复用 0/8/16/24/32 px，基础间距复用 12/16/24/32 px；图片比例、填充及对齐沿用创建预设。每个快捷值只修改当前字段与已说明的必要关联字段，例如圆形需要等宽等高，不能顺带重套整张风格。没有对应常用预设的参数直接提供清楚的数值输入。
4. **精确输入不受预设档位限制**：例如圆角可填 13 px，间距可填 18 px；显示当前真实值和单位。超界、无效或不可解析的输入就近说明并保留待修正文本，不能失焦后静默夹紧、回退成旧值或伪装已应用。当前字段的合法范围与合同一致。
5. **有效操作即时可见**：选择快捷值或有效输入形成预览，画布、结构与属性使用同一状态。一次拖动、一次确认输入或一次快捷应用构成一次撤销操作，不按每个鼠标事件堆积历史；未完成预览不得写入保存请求。
6. **画布是便捷入口，属性是完整精调入口**：拖动、尺寸柄、列分隔线或间距工具只作用于当前适用对象，并显示尺寸、方向或落点。相同意图有可点击和键盘可用的属性/菜单入口；流式对象以排列、排序和分配调整位置，自由布局对象才提供有效的 X/Y。
7. **恢复动作写清恢复到哪里**：“撤销”回退上一条完成操作，“取消本次调整”撤回未提交预览，“恢复继承”移除当前字段的本端覆盖，“恢复最近保存值”使用该范围的保存基线。重新应用一个预设是新的可撤销修改，不是重新生成模板。当前模式不适用的恢复项不显示成可点击假按钮。
8. **把修正入口放在问题旁边**：文字放不下时定位文字，并提供改文本区宽度、字号或内容的入口；间距过密定位父组；图片裁切定位图片填充/取景；对象锁定显示锁定来源。不能只弹“调整失败”或让用户回到新建向导猜下一步。

模板整体预设与局部微调分开：选择一个局部圆角、间距或颜色，默认只改当前对象。若提供“应用到整张模板”或覆盖多个已精调对象的样式操作，须先展示作用范围、受影响参数与保留项，经该明确动作应用后可整体撤销；不能静默覆盖局部值。涉及结构替换时继续复用现有对象与字段映射边界，不用初始配方直接覆盖当前模板定义。

### 9.4 第三阶段：检查、修正与保存

- 从当前精调状态进入只读预览，查看目标尺寸与桌面/平板/手机效果；返回后保持原选中对象、设备及合理视图位置，不从初始预设重新生成。
- 检查至少区分结构/输入错误、影响呈现的质量问题及待完善资料。问题应定位到具体对象并说明修正方向，不能用单一“完成百分比”代替实际结果；人工勾选不能代替机器校验。
- 发布检查只阻断必填资料缺失、非法结构/数值及无法消费的字段冲突；模板名称必须填写，用途和说明无需额外补填。人工预览核对是可选辅助，不作为错误或发布前置条件。潜在外观问题以可定位的建议默认收起，不计入阻断数量；已由自由布局外框限定的图片不得重复要求高度。根属性直接显示名称，顶部提供模板设置入口。
- 初次生成必须满足第 11 节最低质量。生成后允许持续修改并保存合法的未完成草稿；未提交或非法输入需先处理，不能把坏值或拖动中间态当作保存成功。发布是否允许仍由独立发布门禁判断，微调工具和普通保存不被新建步骤进度锁定。
- 明确区分“本次调整已应用”“模板有未保存修改”“模板已保存”和“模板已发布”。保存失败保留精调结果和未保存状态；再次打开读取最终定义，保留具体非预设数值、默认内容、对象身份、排序及适用的设备覆盖。
- 本次补充定义目标功能和验收，不代表所有微调已经实现。创建成功、旧测试通过或属性控件存在，均不能代替第二阶段的逐项实测。

### 9.5 功能区域：默认呈现、就近操作与返回路径

本节细化生成后的功能组织，功能范围沿用第 9.2 节，输入和恢复沿用第 9.3～9.4 节。四区比例、区域正式标题及窄屏行为继续遵守 PROJECT_RULES §7、UI_GUIDE 与从属细则第 7 节；不新增永久功能区。表中的“模板列表”“模板结构”是区域内的任务名称。

| 区域 | 默认呈现 | 主要操作及完成后的去向 |
| --- | --- | --- |
| 模板列表 | 当前模板身份、目录名称与状态、搜索和新建入口；未保存的新模板明确显示为当前会话，不冒充已入库记录 | 点击其他模板先保护当前输入与草稿；切换成功后四区共同更新。复制、重命名、移入回收站等放在具体模板的对象菜单，结果不混同于节点操作 |
| 模板结构 | 按真实父子关系显示区域、分组、图片、文字和按钮；突出当前对象、锁定/隐藏及问题状态；提供“添加区域”“添加元素” | 点行选中，点箭头展开；对象菜单提供复制、同级上移/下移、移动到、显隐、锁定和删除。新增后定位并选中新对象，直接显示它的常用属性；连续添加保留明确的父级与插入位置 |
| 中心画布 | 当前定义的实际效果、适用的选中边界与尺寸反馈；视图工具提供适应画布、缩放和实际尺寸查看 | 点击对象与结构/属性同步；重叠或嵌套对象可借助父级路径和结构树选择。拖动/缩放只对合法对象生效；只读预览中移除编辑手柄，返回时恢复上下文 |
| 属性编辑 | 当前对象名称、所属区域、设备及作用范围；“设计”“页面开放范围”分工明确，默认进入当前对象的常用设计项 | 快捷值和精确输入在同一字段组完成；只读、锁定、继承、混合值均显示真实状态。切换对象前处理未完成输入；修改结果同时反馈到画布、结构和未保存状态 |

顶部工具栏承载当前模板身份与保存状态，以及撤销/重做、设备、预览、保存草稿和独立发布动作；视图缩放可放在画布就近工具条。对象字号、圆角、间距留在属性面板，就近操作栏只提供当前对象的高频快捷动作。重复入口必须使用同一命令、校验与撤销历史，不能出现两个不同结果的“保存”或“删除”。

**属性编辑按对象固定排序，不让用户在参数堆中找入口：**

| 当前对象 | 首个常用组 | 后续适用组 |
| --- | --- | --- |
| 模板整体 | 画布尺寸与比例、背景、整体留白 | 其他整体设计参数；模板名称、用途等资料由明确的模板设置入口维护 |
| 区域/布局组 | 排列方向、分列比例、对齐、间距与内边距 | 区域尺寸、背景与边框、设备覆盖 |
| 图片/Logo | 默认图片选择或替换、填充方式、外框比例；裁切填满时提供有效取景入口 | 外框尺寸、圆角与透明度、设备覆盖 |
| 标题/正文/价格等文字 | 默认文案、字号、字重、颜色 | 字体、行高、字距、对齐、文本框尺寸与换行、设备覆盖；价格角色不接入真实交易 |
| 按钮 | 默认文案、文字色与背景色、尺寸策略 | 内边距、圆角、文字样式与对齐、设备覆盖；页面行动目标位于开放范围 |
| 多选对象 | 对象数量、实际可修改范围、共同属性及混合状态 | 仅显示合法约束交集；无共同属性时引导单选或选择父组，不制造可点击假控件 |

常用组直接展开；后续组按任务具名，例如“文字排版”“外框与圆角”“设备覆盖”，避免只写“高级设置”。分组标题保留当前值摘要、已修改与错误状态。属性顺序不随最近修改自动跳动；页面开放范围不与模板默认内容编辑混在同一表单。恢复操作紧邻其作用字段或组，并标明恢复目标。

**补充元素是可重复的局部操作：** 从结构区或画布就近入口选择目标区域，再选择图片、文字、按钮或适用布局项；选择即插入具有合理尺寸、间距和样式的可编辑对象，沿用当前模板和父组中适用的设计值。首次落点、即将增加的对象和必要包装组须可见，不能自动切回创建向导或重套整张风格。未选中合法容器时先确定目标；失败不留下半个节点或孤立字段。局部添加的校验、连续添加和一次撤销按从属细则第 4.3 节执行。

**以下状态必须有确定的界面结果：**

| 状态或操作 | 区域响应与恢复 |
| --- | --- |
| 生成完成首次进入 | 展示完整基础模板、当前会话与未保存状态；保留合法已有选择，没有选择时显示“选择画布中的元素进行调整”和模板设置入口，不擅自选中第一项 |
| 未选中对象/点击画布空白 | 不显示上一对象的可写表单；保留整体设置入口。清除选择须先处理当前未完成输入，不能把输入写到根节点 |
| 隐藏或锁定对象 | 结构中仍可定位；属性显示状态、来源及有权限时的恢复入口。隐藏对象不因选中自动显示；父级锁定不能被子级按钮绕过 |
| 删除选中对象 | 完成一次可撤销删除后选中仍存在的父级；父级不适用则清除选择。不能残留已删除对象的属性；撤销恢复对象和合理选择 |
| 手动删空模板或区域 | 显示对应的局部添加入口，保留当前模板身份与撤销能力；不自动生成新模板，不把合法空草稿冒充合格生成结果 |
| 进入检查并定位问题 | 第四区临时展示问题列表；点击问题展开结构祖先、定位对象并打开相应字段组。保留“返回检查”及问题上下文，检查重新计算后才更新结果，不要求重新走七步 |
| 保存失败或返回编辑 | 保留当前定义、有效修改和未保存状态，提示具体可执行下一步；只读预览与加载动作不触发保存，不自动覆盖并发版本 |
| 窄屏切换区域/键盘操作 | 使用既定抽屉并保留对象、设备和输入；返回有合理焦点。菜单、添加、排序、尺寸输入和检查定位均有无需拖拽的路径，快捷键不截获正常文字编辑 |

画布“缩放视图”和对象“修改尺寸”必须分别命名：适应画布或查看 100% 不改变逻辑宽高、未保存状态或历史；对象宽高调整才写入模板。设备切换只改变查看上下文，实际本端覆盖须通过明确编辑产生。功能区文案使用对象名与动作，不向用户展示配方、Schema 或内部节点类型等实现细节。

## 10. 未来快速创建

快速创建是后续能力，本轮标准七步流程的验收不要求它已实现。未来可提供“快速创建”和“自定义创建”：前者选择类型后展示推荐摘要，经“快速创建”明确动作生成；后者进入完整七步。两者必须消费同一生成器、值解析和质量校验。

商品促销快速创建示例：4:5、上图下文、1 主图、主标题 + 副标题 + 价格 + CTA、简约。快速模式的简化内容与标准模式推荐中的原价等差异必须显示，不能用入口差异隐式覆盖用户选择。

## 11. 最低质量与可观察验收

此处是持续适用的验收合同，按当前代码和对应运行证据逐项判定，已验证范围见 CURRENT_STATE。工程检查、真实界面、Mock、真实保存重开及公开 Renderer 分别留证；本次新增或细化的微调要求须另行对应验证，不用规则文件更新或旧通过数量充当功能完成证明。

| 验收项 | 必须证明的结果 |
| --- | --- |
| 流程 | 新建进入七步；返回、修改、取消、非法输入、生成中与失败可恢复；确认前不创建、不保存、不发布 |
| 操作次数与范围 | 重选类型或尺寸只更新一个模板级设置；可重复添加区域、图片、同类文字与按钮且身份独立；单槽位修改不新增，最终重复点击不建多个模板 |
| 点击与预览 | 未选用途为中性示例；布局线框读取实际生成几何、文字用短线简化；第 1～6 步选择逐步反映在方案画布；选项与预览切换不丢输入，错误预览不冒充最新；第 7 步生成结果与确认预览一致 |
| 调整可发现性 | 能从“尺寸不合适、图片太多、文字太大、间距太挤”等问题定位对应对象与属性；无需重做向导，局部添加可一次撤销 |
| 阶段完整性 | 七步只属于基础模板创建；生成后可直接使用第 9.2 节微调工具，顺序不强制，可反复检查、修正和保存，不出现第二套必走向导 |
| 区域联动与入口 | 按第 9.5 节从结构树、画布分别选择图片/文字/父组，属性首组正确且选择一致；添加完成自动定位，列表动作不改节点，节点动作不改变模板身份 |
| 区域状态与返回 | 无选择、隐藏/锁定、删除/撤销、删空、问题定位/返回检查、预览返回、窄屏切换均保留合法上下文；无旧对象残留表单，无拖拽也可完成常用微调 |
| 视图与数据分离 | 适应画布、100% 查看、树展开、分组展开及只读预览不新增设计修改；带待提交输入时先按第 9.3 节处理，该次合法提交与视图动作的结果分别可辨 |
| 微调功能覆盖 | 对整体、父组、图片、文字、按钮、元素操作、设备与开放范围逐类验证第 9.2 节功能；每项记录对象、有效值、实际效果、失败/取消及保存回读结果，不以面板存在视为通过 |
| 快捷与精确值 | 同一对象使用圆角 16→13、间距 16→18 等非预设值，画布与属性一致；改一个字段不重套风格；非法输入保留可修正文本且不污染最终定义 |
| 对象与影响范围 | 两张图片、两段同类文字分别调整互不覆盖；布局组间距只影响该组，图片取景不改变外框或源文件；多选、锁定、流式/自由定位均有明确范围 |
| 完整调整事务 | 一次手势或一次应用对应一次撤销，取消零提交；拖拽与非拖拽入口得到同样参数和可见效果；切换对象/设备、进入预览或保存时不遗留未处理输入 |
| 非破坏性精调 | 调整图文、局部外观及根尺寸后仍保留模板与槽位身份、默认内容和无关字段；显式整体样式应用可审查范围并一次撤销，打开和保存不重跑初始配方 |
| 选项完整性 | 9 种类型、8 种固定尺寸及自定义、10 种布局、全部图片与内容选项、全部视觉预设可达；用户覆盖不会被推荐强制重置 |
| 推荐与自由选择 | 商品促销采用 16:9、左右分栏、三图、无价格仍合法；通用/自定义无强推荐；自由布局生成已选内容并可继续自由调整 |
| 图片结构与 Logo | 布局步骤覆盖无图片、自定义 1 和 6 槽位、超界/矛盾配置、背景单图、主图 + Logo、启用/取消 Logo 与结构恢复；总数与真实节点一致，Logo 不重复；图片和内容步骤不增删图片 |
| 固定图片排列 | 新建默认横向；双图、三图、一主多副切换横向/纵向有明确结果，改变单图比例不翻转已选方向；历史省略规则与 auto 输出兼容，已保存定义不重新生成 |
| 数值确定性 | 相同配方产生相同设计参数和节点顺序（新身份除外）；尺寸锁比、类型切换、用户覆盖和摘要与输出一致；不存在只保存预设名却未应用参数的假配置 |
| 边界与层级 | 所有元素在画布内；无异常覆盖或裁切；背景叠加前后景明确；标题、副标题、正文、辅助和价格层级可辨 |
| 间距与对齐 | 相关元素对齐，基础/区域间距统一；内容边距随画布计算；极端横竖比例、多图、多字段组合不丢内容 |
| 图片稳定性 | 图片比例合理，无图与缺图不坍塌；填满/完整显示/圆形/圆角/自由比例可见且可编辑 |
| 可读性 | 文字、按钮和背景满足 UI_GUIDE 的适用对比度；品牌派生色、深浅模式和背景图覆盖分别验证；长文换行且不靠过度缩小或隐藏通过 |
| 编辑自由度 | 生成后可直接选中核心图片与文字；圆角 16→13→27→0、字号、位置、图片填充、最大字数和可替换设置可改；取消/撤销/重做与画布同步 |
| 模板列表 | 新建、切换、复制、重命名、删除/恢复的目标与失败结果明确；复制独立身份、来源不变，历史页面引用不受影响 |
| 保存与隔离 | 生成后的显式保存与重开保留所有修改；示例不泄漏；生成与保存失败不丢源草稿；发布仍需独立确认 |
| 三端与三种消费面 | 设计基准尺寸及桌面/平板/手机下，编辑画布、模板预览和公开只读 Renderer 的实际节点顺序、尺寸、间距、取景和显隐一致；不能只验外层属性或 mounted 状态 |
| 运营完成任务 | 运营人员完成“选项创建 → 看到可用结果 → 画布/属性微调 → 保存重开”，记录困惑、恢复与协助；不要求先学从零拖拽 |

后续实现以全量预设可达检查加风险组合覆盖验收，不虚构已穷举全部组合。至少覆盖无图纯文、6 图、多字段长文、全幅背景、自定义极端比例、重复 Logo、非推荐组合和生成后非预设数值修改。不可满足的输入在生成前明确阻止并说明如何修正；不能静默丢弃内容、输出越界模板或把失败留给首次编辑。

所有新增模板功能遵循：步骤化、选项化、数值化、自动化、可编辑、标准化、低依赖画布。旧固定模板数量、旧空白创建测试和旧 PASS 不构成这份新规则的验收证据。
