# 海川珠宝项目体检报告（2026-08-30 修订版）

> 修订对象：用户提供的《海川珠宝项目体检报告（2026-08-30）》及其后续复核结论。
>
> 修订原则：保留可复现事实，撤回或降级无法复现、口径不清、跨证据层外推的结论；原始报告保留，不在原附件上覆盖修改。
>
> 证据时点：`2026-08-30T19:15:39+08:00`（Asia/Shanghai）。
>
> 评估对象：分支 `codex/release-curation-20260814` 的 HEAD `f754c3bdfd3aa3c171ac0ea968e4d800b77d536b`，以及证据时点同一工作树中的 staged、unstaged 和 untracked 状态。

## 1. 管理层结论

当前工作树包含大量候选实现、测试资产和工程设施，但现有证据不足以宣称“完整质量门禁通过”“生产环境就绪”或“可以直接发布”。当前最需要收敛的不是继续扩张功能，而是恢复可追溯基线、在受支持运行时复验、刷新候选证据，并把目标环境、正式内容和业务闭环分别验收。

核心裁决如下：

1. 前台页面装修唯一采用 Puck `PageDocument`。统一 V2 是该单一体系内部的版本化模板定义层，包含独立模板工作空间、`TemplateDefinition`、节点注册表、动态结构树、通用 Renderer 和服务端激活门禁；它不是 `PageDocument` 的替代体系，也不得形成第二套可创建、编辑或发布的内容事实源。
2. 当前 HEAD 的已提交历史存在远端可达证据，但当前证据不能证明全部工作树改动已经由远端 Git 快照覆盖。其中哪些变化已包含于远端 hotfix、哪些只存在于 index 或工作树，仍需来源审计确认；不能断言全部成果只存在于一台机器。
3. 上一轮对话记录称 typecheck、服务端测试、客户端构建和 bundle 检查在 Node `25.2.1` 下退出码为 0，但本报告没有独立复核对应日志。项目正式要求 Node.js `22.12+` 且保持 `22.x`，因此这些记录不能作为正式门禁通过证明。
4. 当前候选证据已经漂移；目标环境身份、数据库 migration、域名、TLS、真实证书、真实内容、恢复演练和生产行为均未由本轮验证。
5. 当前状态文档和既有审计记录了页面装修的合同、服务和浏览器检查，本轮没有重跑；目标环境 Gate B 尚未执行，动态模板正式激活也没有生产证据。正确工作不是维持两套装修体系，而是在单一 `PageDocument` 体系中继续保持失败关闭并完成环境门禁。
6. `SalesMode`、订单、支付和报价相关代码存在，不等于标准零售、高级定制和合作蜡模的完整业务闭环已经完成；完整性必须按客户本人确认、报价版本、资源门禁、事务转单、不可变快照、支付和履约分别验收。

当前建议状态：

> 可以规划“工作树来源审计、Node 22 正式复验、候选证据刷新、目标环境只读审计和正式内容补齐”阶段；其中只读审计可直接开展，Git 写入、migration、环境修改、动态模板激活和发布仍分别等待对应明确授权。现有证据不足以宣称发布就绪。

## 2. 取证范围与保护边界

本轮创建了未跟踪文档 `docs/acceptance/PROJECT_HEALTH_AUDIT_2026-08-30_REVISED.md`；除此之外没有修改源代码、配置、Git index、Git 历史或业务状态。

本修订只使用以下证据：

- 当前工作树中的治理规则、代码、配置、机器合同和状态文档；
- 新鲜只读 Git 图与工作树统计；
- 新鲜 Node/npm 版本检查；
- 对现有公开素材审计和运行时所有权审计文件执行的只读 `--check`；
- 上一轮对话记录中声称已执行、但本报告没有独立复核的构建和测试结果；这些内容不作为本轮验证证据。

本轮没有执行：

- `lint`、`typecheck`、`build`、应用测试或 Playwright；
- Docker、浏览器、HTTP、真实数据库、目标环境或生产环境检查；
- `.env` 真实值读取；
- Git 暂存、commit、push、PR、合并或历史改写；
- migration、部署、发布、环境变量修改或真实业务数据写入；
- 对任何现有浏览器装修草稿的保存、丢弃、恢复、刷新、导航、发布或自动化操作。

如果浏览器中存在“已同步到画布，尚未保存草稿”或其他来源不明的未保存状态，它们继续视为用户资产。后续浏览器验收必须使用不会触碰该会话的独立干净环境。

## 3. 本轮新鲜证据快照

### 3.1 Git

以下统计取自证据时点 `2026-08-30T19:15:39+08:00` 的同一工作树；统计包含当时已创建的本修订报告：

| 项目 | 当前值 | 口径 |
| --- | ---: | --- |
| `git status` 默认记录 | 294 | porcelain v1，每条状态记录计一次 |
| 工作树唯一路径 | 309 | staged、unstaged、untracked 路径合并去重，包含本修订报告 |
| staged-only | 150 | 只在 index 中变化 |
| unstaged-only | 117 | 只在工作树中变化 |
| mixed | 10 | 同一路径同时有 staged 与 unstaged 变化 |
| untracked | 32 | Git 未跟踪路径；包含本修订报告，不推定可删除 |

提交图快照：

- `main...HEAD`：`0 / 82`，即 `main` 无独有提交、当前 HEAD 独有 82 个提交；
- `origin/codex/release-curation-20260814...HEAD`：`0 / 10`；
- 2026-08-30 本轮只读查询到远端 `codex/prod-frontend-hotfix-20260830` 为 `98d8d4a6c65bbac6e515467090d517d7596a2265`，包含当前 HEAD 并在其后增加 4 个提交；
- 该 hotfix 相对 `main` 共多 86 个提交。

这些事实只能证明相应 commit 对象的 Git 可达性。它们不能证明全部工作树变化已由远端 Git 覆盖、已合并 `main`、已部署或已安全整合。

### 3.2 工具运行时

- 当前 shell：Node `v25.2.1`、npm `11.6.2`；
- 项目规则：Node.js `22.12+` 且保持 `22.x`；
- 根、`client/`、`server/` 的 `package.json` 都声明 `22.x`，但根声明没有单独表达 `22.12+` 的最低小版本，执行时仍以项目规则为准。

因此，上一轮在 Node 25 下取得的退出码 0 只能作为辅助诊断，不能提升为正式质量门禁证据。

### 3.3 候选证据

现有 `artifacts/candidate-evidence/current.json`：

- 生成于 `2026-08-29T06:43:05.097Z`；
- 证据类型是 `LOCAL_WORKTREE_CANDIDATE`；
- `releaseEligible=false` 是生成器对本地候选证据固定写入的合同值，不是对目标环境是否存在的探测结果；
- 文件记录的是旧工作树快照，不能代表本报告时点。

本轮只读检查结果：

| 命令 | 退出码 | 关键结果 | 能证明 | 不能证明 |
| --- | ---: | --- | --- | --- |
| `npm run verify:public-asset-audit` | 1 | `PUBLIC_ASSET_AUDIT_STALE` | 现有公开素材审计文件与当前工作树不一致 | 公开素材功能失败、生产素材状态 |
| `npm run verify:runtime-ownership-audit` | 1 | `RUNTIME_OWNERSHIP_AUDIT_STALE` | 现有运行时所有权审计文件与当前工作树不一致 | 运行时功能失败、目标环境归属 |

两个检查均运行在当前 Node 25 shell，只执行静态证据比较。完整候选检查还会进入 Docker inspect，本轮没有执行。

### 3.4 命令账本

| 检查 | 命令或口径 | 退出码 | 关键结果 |
| --- | --- | ---: | --- |
| 分支与 HEAD | `git branch --show-current`；`git rev-parse HEAD` | 0 | `codex/release-curation-20260814`；`f754c3b...` |
| 默认状态记录 | `git status --porcelain=v1 --untracked-files=normal` 后按输出记录计数 | 0 | 294 |
| 工作树路径分类 | 分别读取 `git diff --cached --name-only`、`git diff --name-only`、`git ls-files --others --exclude-standard`，再用路径集合求 staged-only、unstaged-only、mixed、untracked 与并集 | 0 | 150 / 117 / 10 / 32；并集 309 |
| 提交图 | `git rev-list --left-right --count main...HEAD`；对同名 upstream 和 hotfix SHA 执行同类比较 | 0 | `0/82`；`0/10`；hotfix 相对 HEAD `0/4` |
| 远端指定引用 | `git ls-remote --heads origin refs/heads/main refs/heads/codex/release-curation-20260814 refs/heads/codex/prod-frontend-hotfix-20260830` | 0 | `53a6725...`；`9962521...`；`98d8d4a...` |
| hotfix 包含 HEAD | `git merge-base --is-ancestor HEAD 98d8d4a6c65bbac6e515467090d517d7596a2265` | 0 | 当前 HEAD 是该 hotfix SHA 的祖先 |
| 工具版本 | `node --version`；`npm --version` | 0 | `v25.2.1`；`11.6.2` |
| 公开素材审计 | `npm run verify:public-asset-audit` | 1 | `PUBLIC_ASSET_AUDIT_STALE` |
| 运行时所有权审计 | `npm run verify:runtime-ownership-audit` | 1 | `RUNTIME_OWNERSHIP_AUDIT_STALE` |

## 4. 对原报告的关键修正

| 原报告表述 | 修订裁决 | 正确写法 |
| --- | --- | --- |
| “292 个文件未提交” | 计数口径不明，且动态数字已变化 | 同时记录 porcelain 状态记录和 staged/unstaged/untracked 去重路径，并附时间、HEAD 和命令口径 |
| “全部成果压在一台机器” | 过度结论 | 已提交历史有远端可达证据；当前证据不能证明全部工作树变化均由远端覆盖，其他系统级备份也未验证 |
| “V1 Puck + V2 动态模板双体系迁移” | 架构描述错误 | 唯一体系是 Puck `PageDocument`；V2 是体系内部的版本化模板定义层 |
| “目标环境不存在” | 证据不足 | 本轮没有取得目标环境身份和验收事实，因此目标环境状态未验证 |
| “本地 5 容器健康运行” | 可能是历史运行快照 | 本轮未检查 Docker；不得把旧容器状态外推为候选分支或生产状态 |
| “交易域代码完成” | 完成度过度 | 存在部分代码与安全关闭路径；三报价、本人确认、事务转单、不可变快照、真实支付与履约闭环仍需分别验收 |
| “经营资料 11 类空缺” | 计数规则不可复现 | 列出缺失资料本身，并明确顶层条目、子项或业务类别的计数方法；数量不影响决策时不写固定总数 |
| “代码已验证/暴露面待验证” | 范围仍过宽 | JWT、Guard、uploads 和日志实现只能证明对应代码形态；漏洞必须按具体入口、数据分类、调用或滥用途径、实际影响与现有缓解措施分别验证 |

## 5. 当前项目状态地图

| 范围 | 修订后状态 | 证据与限制 |
| --- | --- | --- |
| 页面装修架构 | 已确认单一事实源 | `AGENTS.md`、`PROJECT_RULES.md` 和当前代码均以 Puck `PageDocument` 为唯一活跃体系 |
| 页面装修代码侧闭环 | 存在既有记录，本轮未验证 | `docs/CURRENT_STATE.md` 和店铺装修闭环审计记录了合同、服务、UI-Mock 与隔离真实 API 结果；不能替代本轮或生产验收 |
| 动态模板激活 | 失败关闭；目标环境未验收 | 当前状态文档记录 Gate B 只读入口存在，但本轮未复核其运行；正式激活仍需 migration、回滚准备、明确批准和相称验收 |
| 客户前台与正式内容 | 当前运行状态未复核 | 状态文档记录历史 revision 已被安全降级、部分页面没有有效发布稿；本轮未访问 HTTP 或浏览器 |
| 交易与报价 | 部分实现，完整闭环未成立 | D.19 明确三报价、本人确认、分通道资源门禁、事务转单和完整快照仍有实现或审计缺口 |
| 质量门禁 | 当前未确认 | 同日状态文档记录过更完整检查，但本轮未在 Node 22.12+ / 22.x 下重跑完整 `quality.yml` 与 Playwright |
| 候选证据 | 阻断 | 两个现有审计文件均 stale；本地候选证据不是发布清单 |
| 目标环境与生产 | 未验证 | 未核对目标数据库、域名、TLS、证书内容、恢复演练、监控、备份或生产发布 |

“Gate B”是当前计划和脚本使用的阶段名称，不是永久项目规则；底层门禁以 `PROJECT_RULES.md` §7 为准：目标环境 migration、回滚准备、明确批准和相称验收完成前，服务端原子激活必须保持失败关闭。

## 6. 主要问题与优先级

本节 P1 只表示本报告证据时点的建议优先级，不构成永久产品规则或执行授权。

### P1：恢复可追溯基线

当前工作树同时包含 staged、unstaged、mixed 和 untracked 资产，且 hotfix、HEAD、index 与工作树存在交叉。后续不应直接按目录批量提交，也不得把未跟踪文件推定为可删除。

正确动作是先进行四方来源审计，以确认哪些变化已经由 hotfix 覆盖、哪些仍只存在于 index、工作树或未跟踪资产：

1. 远端 hotfix；
2. 当前 HEAD；
3. index；
4. 工作树与未跟踪资产。

按逻辑意图识别每项来源、依赖和验证证据后，才能在获得明确 Git 授权时精确暂存和提交。本地 commit、push、PR、`main` 集成和发布是不同动作，必须分别遵守各自授权边界。

### P1：在受支持运行时重建正式门禁证据

切换到 Node.js `22.12+` 的 `22.x` 后，应以当前 `.github/workflows/quality.yml`、包脚本和 `WORKFLOW.md` 为准重跑适用门禁。至少必须明确覆盖：

- 根 `lint`，并注明它当前只检查客户端；
- 根 `typecheck`；
- 根 `test` 的完整组合；
- 前后端生产构建和 bundle 预算；
- 独立 Playwright job；
- 涉及 Puck 共享 Renderer、合同结构、顺序或高度时，所有活跃模板的真实浏览器自动 DOM、顺序和高度检查，以及相称的桌面/移动人工抽样。

构建不能证明视觉，Mock 不能证明真实接口，本地成功不能证明生产可用。

### P1：刷新候选证据并保持失败关闭

先重新生成并核对公开素材审计、运行时所有权审计和候选证据，再评估候选身份。不得通过空证书目录、伪挂载、跳过测试或降低门禁条件制造绿色结果。

本轮静态复核 `scripts/verify-environment-contract.mjs` 的 `hasReadOnlyWechatCertificateMount`：当前逻辑只从 Compose 文本识别证书相关只读挂载，并直接据此设置 `readyForRealWechatPayments`。正确增强方向是验证实际证书文件存在、非空、格式和读取权限，并补充空目录、缺文件和无效文件负向测试。该增强属于后续实现任务，本报告没有修改验证器。

### P1：建立目标环境与发布事实

需要业务和运维明确目标环境身份、正式域名、TLS 终结方式、证书存储与续期负责人、目标数据库、备份与回滚方式。随后按只读 Gate B、获批 migration、恢复演练、构建制品、健康检查和业务验收逐层推进。

不能从 `releaseEligible=false`、本地 Docker 或示例配置推出目标环境不存在、已部署或可用。

### P1：补齐正式内容和业务闭环

正式商品、页面内容、联系资料、经营主体、协议、备案与支付主体资料应建立责任人和验收清单；不再用未定义口径的“11 类”概括。

标准零售、高级定制和合作蜡模必须按 D.19 分别验收客户资格、报价、本人确认、资源门禁、事务转单、不可变快照、支付和履约。功能开关继续安全默认关闭，直到对应目标环境和业务验收完成并获得授权。

## 7. 待专项安全验证，不作为已确认漏洞

以下线索来自上一轮复核记录；本轮没有重新读取其完整调用链或执行动态验证。它们仅作为待复核候选，不代表当前代码仍然存在对应问题，也不构成已确认漏洞：

- 微信 OAuth `state` 是否需要更强的会话绑定和一次性消费；
- 公开 uploads 中是否存在历史敏感文件；
- 每个后台敏感路由是否都有与业务风险相称的服务端角色或权限白名单；
- JWT 用途隔离是否达到目标部署要求。

`OperationLog` 不记录 raw body 可能是数据最小化处理。在没有审计需求、数据分类规则和具体缺失字段证据前，不将其定性为缺陷或正确设计。

上述候选若要升级为漏洞或修复任务，必须另行取得具体入口、数据分类、可验证的调用或滥用途径、实际影响和现有缓解措施证据；不要求为了证明问题而执行破坏性 PoC。

## 8. 推荐执行顺序

1. 保持现有浏览器未保存草稿不动；在来源审计完成前，暂停与本次归并范围重叠或属于同一逻辑区域的新增写入。独立且不重叠的任务只有在所有权清晰时才可继续。
2. 对 hotfix、HEAD、index、工作树和 untracked 资产做来源审计，形成可独立验收的逻辑批次。
3. 获得相应 Git 授权后，逐批验证、精确暂存并创建本地 commit；push、PR、合并和发布分别处理。
4. 在 Node.js `22.12+`、`22.x` 环境复现完整质量门禁和独立 Playwright。
5. 刷新公开素材、运行时所有权和候选证据，保持任何缺失都失败关闭。
6. 明确目标环境后先执行只读 Gate B；migration、动态模板激活、环境变量和部署继续等待各自明确授权。
7. 域名、TLS/证书、恢复演练、正式内容与经营资料可以按依赖并行准备，但不能互相替代验收。
8. 三种业务通道按真实客户旅程分阶段闭环；资金能力最后独立开放。

## 9. 验证记录与未验证项

| 检查 | 本轮结果 | 结论边界 |
| --- | --- | --- |
| 当前治理规则、`WORKFLOW.md`、`PROJECT_RULES.md` | 已读取并交叉核对 | 证明本报告使用当前工作副本规则；不证明其他既有任务已热加载 |
| Git 状态、提交图、远端指定 ref | 已只读核对 | 证明本报告时点的 Git 关系；不证明 Git 之外的备份或部署 |
| Node/npm 版本 | Node `25.2.1`、npm `11.6.2` | 证明当前 shell 不符合项目应用运行时要求 |
| 公开素材审计文件 `--check` | 退出码 1，stale | 证明证据文件已漂移，不证明业务功能失败 |
| 运行时所有权审计文件 `--check` | 退出码 1，stale | 证明证据文件已漂移，不证明目标环境归属 |
| 环境合同证书 readiness 逻辑 | 已静态读取 `scripts/verify-environment-contract.mjs` | 证明当前 readiness 只消费挂载文本条件；不证明真实证书文件有效或支付可用 |
| 上一轮对话声称的 typecheck/server test/client build/bundle | 本报告未独立复核；记录称 Node 25 下退出码 0 | 不作为本轮验证证据；需从实际日志或 Node 22.12+ 重跑结果建立正式证据 |
| 根 lint、根 test、完整 `quality.yml`、Playwright | 本轮未运行 | 当前正式质量门禁未确认 |
| Puck 所有活跃模板浏览器矩阵 | 本轮未运行 | 编辑器、预览、公开 Renderer 的 DOM 顺序、高度、交互及桌面/移动视觉一致性未确认 |
| 完整 candidate-evidence 检查 | 本轮未运行，因为后续步骤会进入 Docker inspect | 候选身份和完整发布证据未确认 |
| Docker、HTTP、浏览器、真实数据库 | 本轮未检查 | 当前运行态和真实闭环未确认 |
| 目标环境、域名、TLS、证书、恢复、生产发布 | 本轮未检查 | 生产可用性未确认 |
| 本修订文档自身 | 已完成路径和章节引用、重复/冲突、过时事实、授权与失败场景、标题/表格结构、占位符、空白字符及未跟踪文件差异检查 | 引用目标缺失 0、占位符 0、表格行问题 0、行尾空白 0；`git diff --no-index --check` 无空白错误输出，其退出码 1 来自新文件与空文件存在预期差异 |

## 10. 授权边界

本报告只修订诊断结论，不授权或执行以下动作：

- 保存、丢弃、恢复、刷新、导航或发布浏览器草稿；
- 删除、移动或清理现有文件与未跟踪资产；
- Git 暂存、commit、push、PR、合并或历史改写；
- Schema/migration、真实数据修改、环境变量或密钥写入；
- 动态模板正式激活、部署或生产发布。

执行到上述任一步骤时，以用户对精确目标、范围、环境和动作的当前明确授权为准。
