# 海川珠宝 — 项目与品牌基线

> 本文件只定义长期稳定的产品、品牌与技术边界，不记录当前开关、页面数量、测试数量或临时上线状态。
> 当前事实见 `docs/CURRENT_STATE.md` 并以代码、配置与实际运行结果复核；执行权限见 `AGENTS.md`。
> 最近核对：2026-08-20（同步全站视觉规范路由；未改变产品、品牌或技术边界）。

## 1. 规则架构

| 权威源 | 唯一职责 |
| --- | --- |
| `AGENTS.md` | 安全、权限、工作区、敏感信息与审批底线 |
| `docs/PROJECT_GUARDRAILS.md` | 项目定位、品牌原则与技术边界 |
| `PROJECT_RULES.md` | 当前技术实现不变量与单一事实来源 |
| `WORKFLOW.md` | 任务执行、验证与交付流程 |
| `docs/DECISIONS.md` | 已批准决策、原因、失效标记与待决策事项 |
| `docs/UI_GUIDE.md` | 全站视觉与体验详细标准：正文为客户前台，附录 A 为管理后台 |
| `docs/CURRENT_STATE.md` | 可变化的当前实现和上线状态，不构成授权 |

规则发生冲突时，不得自行挑选最方便的一条。先遵守 `AGENTS.md` 的安全与审批边界，再核对用户本次明确授权；仍与项目基线或已批准决策冲突时，必须指出冲突并请求确认。事实判断顺序为：实际运行结果 → 当前代码、类型、Schema 与配置 → 已核对状态文档 → 历史记录。

## 2. 产品定位

海川珠宝的客户前台是以珠宝作品、材质工艺、品牌内容与顾问服务为核心的数字品牌空间；内部系统负责商品、内容、客户咨询与运营管理。

客户体验目标：

- 先建立发现、理解与信任，再引导预约鉴赏、定制咨询、选款或其他已批准服务；
- 不做批发目录、促销会场、通用商城模板、SaaS 模板或竞品页面复刻；
- 高级感来自真实作品、摄影、排印、比例、留白和服务细节，不来自大面积金色、仿金属效果或强迫式营销；
- 不以“极简”为由隐藏商品事实、费用、隐私说明、错误状态、服务条件或下一步。

“高级珠宝品牌级体验”是设计目标，不是对品牌历史、世家传承、认证、奖项或行业地位的事实声明。所有对外主张必须有可核验来源。

## 3. 技术基线

以下是经 `package.json` 于 2026-08-17 核对的主版本基线；精确版本与实际可用能力仍以锁文件、代码和运行结果为准。

| 范围 | 基线 |
| --- | --- |
| 前端 | React 18、TypeScript 5、Vite 8 |
| UI 与样式 | Ant Design 5、Tailwind CSS 3、现有全局样式与设计令牌 |
| 前端状态 | Zustand 4 |
| 后端 | NestJS 11 |
| 数据层 | Prisma 5、MySQL 8 |
| 认证 | JWT、bcrypt |
| 图片处理 | Sharp |
| 内容编辑 | 现有 EditorWorkbench 与 Puck PageDocument 体系 |

未经明确批准，不得替换主框架、升级或删除依赖、迁移数据库技术、重建页面装修体系或移动核心目录。是否启用缓存、队列、支付、物流、短信或其他生产服务属于独立决策，不能仅凭代码存在或环境变量默认值推断已获上线授权。

## 4. 业务与发布边界

- 展示、咨询、购物车、结算、支付、退款、库存与合作伙伴能力必须分别说明当前代码状态、验证环境和公开范围；不得用“电商已完成”概括所有能力。
- 交易开关、路由可见、接口放行、支付凭据就绪和生产业务批准是不同事实，不能互相替代。
- 未经明确上线批准，不得开启真实资金处理、生产第三方写入或发布未经核验的经营承诺。
- Mock、演示数据、代码路径存在或本地成功均不证明生产可用。
- 最小公开版本优先保证品牌展示、作品浏览、咨询转化、隐私与联系信息真实可靠；是否开放交易以单独发布决策为准。

## 5. 品牌与体验边界

- 客户前台品牌与体验细则只认 `docs/UI_GUIDE.md` 正文；管理后台排版、密度与无障碍细则只认其附录 A。其他治理文件不得复制色值、字号、间距或动画时长形成第二套规范。
- 页面装修器、CMS 内容和运营模板同样受品牌、真实性、版权、性能、响应式与无障碍门禁约束。
- 管理后台以效率、信息层级、状态完整性和无障碍为先，不全局套用客户前台的大留白、衬线标题或展示动效。
- Logo、核心字体、品牌色、全局设计令牌和整体交互基调的变更属于品牌系统变更，必须明确审批并提供双端验收证据。

## 6. 内容、资产与信任

- 不得编造品牌历史、创始年份、传承、认证、奖项、产地、宝石等级、稀缺性、库存、销量、价格、保值能力或服务时效。
- 图片、字体、视频、音乐、图标、代码和设计稿必须具备可追溯的商用授权；不确定时只能作为内部草稿。
- AI 生成素材不得作为真实珠宝、工艺、证书、佩戴效果或客户案例的证据。
- 客户数据只处理完成当前任务所需的最小范围；具体安全与敏感信息规则以 `AGENTS.md` 为准。

## 7. 维护原则

- 稳定原则写入本文件；当前实现写入 `docs/CURRENT_STATE.md`；决策与理由写入 `docs/DECISIONS.md`；执行方法写入 `WORKFLOW.md`。
- 文档不得用“最近看起来如此”替代代码或运行证据；易变化的数字、开关和模块清单必须带核对日期。
- 旧规则若仍被工具引用，优先改成兼容入口或失效记录，不直接删除文件；确认零引用且经批准后才删除。
- 每条新规则应回答：解决什么风险、适用何处、如何验证、何时允许例外。无法回答的口号式规则不进入权威文件。
