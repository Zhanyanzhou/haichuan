# 待办事项报告 — 需要做但尚未完成的工作

> 生成时间：2026-08-13
> 范围：P0-C → P2-C 全阶段代码层工作已完成，本报告汇总所有**仍未完成**的事项。
> 分类依据：按阻塞原因和责任方分组，便于逐项推进。

---

## A. 需要你（用户）在本机执行的验证 【最高优先级，唯一技术阻塞】

本会话无 Shell 工具，以下命令**必须**在用户本机执行才能完成最终验收。

| # | 命令 | 目的 | 预期 |
|---|---|---|---|
| A-1 | `cd client && npm run build` | 客户端构建（tsc + vite build） | 0 error |
| A-2 | `cd client && npm run lint` | 客户端 ESLint | 0 error（warning 可接受） |
| A-3 | `cd server && npm run build` | 服务端构建（nest build） | 0 error |
| A-4 | `npm run typecheck`（根目录） | 前后端类型检查 | 0 error |
| A-5 | `npm test`（根目录） | page-builder 契约测试 | 通过 |
| A-6 | `npm run test:trade`（根目录） | 交易域状态机/并发/契约测试 | 通过 |
| A-7 | `cd client && npx playwright test --project=public-chromium` | 公开页面 E2E（含新增 privacy-trust） | 全部通过 |
| A-8 | `git diff --check` | 行尾/空白冲突检查 | 无冲突 |

**若任一命令失败**：把报错贴给我，我会在授权范围内立即修复。

**运行 Playwright 前注意**：
- 需先 `cd client && npm install`（若未装 Playwright 浏览器则 `npx playwright install chromium`）
- mock 模式：`$env:VITE_USE_MOCK="true"` 后运行（不依赖后端）
- 真实接口模式：需 3000 端口后端运行 + `$env:PLAYWRIGHT_API_BASE_URL="http://127.0.0.1:3000/api"`

---

## B. 需要老板提供的真实经营资料 【业务阻塞】

代码已就绪，但**前台显示依赖真实数据**。没有这些资料，网站只能"不报错、不造假"，但不能算"上线就绪"。

| # | 事项 | 当前状态 | 填写位置 |
|---|---|---|---|
| B-1 | 真实联系电话 | 空（前台隐藏，显示"公开联系方式正在完善"） | 后台 `/admin/site-content` 联系电话 |
| B-2 | 真实联系邮箱 | 空（隐藏） | 后台 联系邮箱 |
| B-3 | 真实总部/门店地址 | 空（隐藏） | 后台 公司地址 |
| B-4 | 真实营业时间 | 空（隐藏） | 后台 营业时间 |
| B-5 | **正式域名** | 未确认（robots/sitemap/JSON-LD 保持空） | 确认后需补：sitemap.xml 各 URL、robots.txt Sitemap 行、index.html og:url |
| B-6 | 真实运营主体（隐私页） | 未填写（隐私页已注明"需运营主体复核"） | 隐私说明正式上线前由法务/合规复核 |
| B-7 | 品牌真实 Logo | 默认文字 logo | 后台 网站设置 Logo 上传 |
| B-8 | 首页分享图（og:image） | 已移除（引用了不存在的图片） | 运营提供 1200×630 分享图后补回 index.html |

---

## C. 需要部署/环境的工作 【超出本会话授权】

| # | 事项 | 原因 | 需要什么 |
|---|---|---|---|
| C-1 | **重新部署 3000 端口 Docker** | 当前 3000 是旧版本，所有代码改动**尚未部署** | Docker 重新构建 + 发布（需老板授权部署） |
| C-2 | 后端 `/admin/*` 端点回归测试 | admin 密码不在仓库 | 用户提供 admin 密码后跑回归 |
| C-3 | 真实接口 503 交易冻结验证 | 需运行中的新版本后端 | 部署后用 `PLAYWRIGHT_API_BASE_URL` 跑 public-access.spec.ts 真实接口段 |
| C-4 | 真实接口公开字段白名单验证 | 同上 | 部署后跑 public-access.spec.ts "真实接口公开数据契约" 段 |
| C-5 | CORS 默认域名处理 | `server/src/main.ts:40` 默认 `haichuanjewelry.com` | 域名确认后统一处理（基础设施配置，不在自主授权范围） |

---

## D. 需要浏览器才能完成的视觉验收 【P1-B 未完成部分】

代码层 a11y/语义已修复（label 关联、button type、aria），但**视觉层面**必须人眼或浏览器验证。Playwright 测试覆盖了横向溢出和基本交互，但以下需要人工确认：

| # | 页面 | 检查项 | 尺寸 |
|---|---|---|---|
| D-1 | 全部公开页 | 横向溢出（测试已覆盖，但需实跑确认） | 1440/1024/768/390 |
| D-2 | 全部公开页 | 字体大小/留白/信息层级 | 4 断点 |
| D-3 | 全部公开页 | 文本对比度（WCAG AA） | 4 断点 |
| D-4 | 商品详情 | 图片裁切/变形 | 4 断点 |
| D-5 | 移动端菜单 | 开合/焦点陷阱/键盘 | 390 |
| D-6 | 隐私页（新） | 内容排版/联系方式区块 | 4 断点 |
| D-7 | 联系表单（改） | 三态视觉/loading 反馈/error 关联 | 4 断点 |
| D-8 | 选款弹窗（改） | 隐私同意 checkbox 布局/移动端 | 390 |
| D-9 | 页脚（改） | 隐私链接位置/间距 | 4 断点 |
| D-10 | 返回顶部 | 各页面是否存在/可用 | 桌面+移动 |

---

## E. 我识别但未解决的代码层问题（需决策或超出范围）

| # | 事项 | 现状 | 建议 |
|---|---|---|---|
| E-1 | **Playwright 未纳入 CI** | ci.yml/quality.yml 只跑 lint/build/typecheck/契约，不跑 E2E | 需加浏览器安装步骤，属流水线+依赖变更，按工单不擅自改。**建议**：加一个 `playwright` job 缓存浏览器 |
| E-2 | `[antd: message]` 静态方法警告 | 控制台 warning | 低优先级，需改用 App message 实例（涉及多文件，非本次范围） |
| E-3 | Dashboard API 400 错误（历史 P1-6） | 未核实 | 需后端运行 + admin 登录后验证 |
| E-4 | `nest build` 可用性（历史 P2-2） | 未核实 | 需本机 `cd server && npm run build` 确认 |
| E-5 | prisma generate DLL 锁定（历史 P2-3） | 环境项 | Windows 文件锁，停服务后重试 |
| E-6 | 产品名称快照编码（历史 P2-5） | 未核实 | 需真实商品数据测试 |
| E-7 | 首页 FallbackHome 静态精选作品 | `productFocus` 引用真实图片但未关联商品库 | 运营发布真实 Puck 文档后自动覆盖；或后续将 FallbackHome 改为从公开 API 拉取 |
| E-8 | homeCampaign.ts 营销文案 TODO（5 处） | 编辑文案占位，非事实声称 | 运营提供最终 verified copy 后替换 |
| E-9 | Custom 页摄影占位（3 处 TODO） | 流程/材质/案例图占位 | 运营提供真实摄影后替换 |
| E-10 | `storeInfo.puck.tsx` 已清空默认值 | ✓ 已处理，但运营添加该区块时仍需手填 | 编辑器内提示已足够 |

---

## F. 部署前人工验收项（不向真实库写测试数据）

| # | 事项 | 为什么不能自动验证 |
|---|---|---|
| F-1 | 咨询表单真实写入 → 后台线索列表 | 不向真实库写测试线索；需部署后用真实表单提交验收 |
| F-2 | 选款咨询真实写入 → 后台选款管理 | 同上 |
| F-3 | 合作商申请流程（注册 → 申请 → 后台审核 → 降级/升级） | 需多角色联调，部署后验收 |
| F-4 | 客户跨设备选款同步 | 需两个登录会话验证 |
| F-5 | 付款凭证 503 拒绝（真实调用） | 部署后直接 curl 验证 |

---

## G. 长期/未来阶段（当前阶段明确不做）

| # | 事项 | 说明 |
|---|---|---|
| G-1 | 购物车/结算/支付开启 | **老板已决定当前冻结**。开启需完成：支付网关对接、回调验证、库存/价格/退款审计验收、真实资金测试 |
| G-2 | 行为分析开启 | **当前默认关闭**。开启需先完成：独立隐私偏好机制、用户授权、同意版本管理 |
| G-3 | 正式域名上线后补 SEO | canonical URL、sitemap 绝对地址、JSON-LD url、og:url |
| G-4 | 多仓库存管理启用 | DECISIONS D.1/D.9 待决策（库存架构迁移、默认仓库） |
| G-5 | Feature Flags 正式接线或删除 | DECISIONS D.3 待决策 |
| G-6 | 金价自动采集接入 | DECISIONS D.10 待决策（当前空壳 Cron） |

---

## 优先级建议

**立即做（解锁验收）**：
1. A-1 ~ A-8：本机跑验证命令 ← **这是唯一的技术阻塞，做完才能确认代码无编译/测试错误**

**短期做（解锁上线）**：
2. B-1 ~ B-4：老板提供真实联系信息 → 后台填入
3. B-5：老板确认正式域名
4. C-1：重新部署 Docker（使代码改动生效）

**上线前做**：
5. D-1 ~ D-10：浏览器视觉验收
6. F-1 ~ F-5：人工端到端验收
7. C-2 ~ C-4：admin/真实接口回归

**可延后**：
8. E-1 ~ E-10：按优先级逐项处理
9. G-*：按业务节奏推进

---

## 总结

- **代码层**：P0-C → P2-C 全部完成，22 生产文件 + 2 测试文件 + 6 文档已改。
- **唯一技术阻塞**：本会话无 Shell，A 组 8 条验证命令必须用户本机执行。
- **唯一业务阻塞**：B 组真实经营资料需老板提供。
- **其余**：部署/浏览器/长期项按节奏推进，不阻塞当前代码验收。

> 把 A 组命令跑完，把结果（通过或报错）告诉我，我继续处理。
