# 海川珠宝功能完成度矩阵

> 最后整理：2026-09-14。本文是总体验收状态的唯一汇总入口，只记录当前结论、证据层和关闭条件；功能清单、路由、测试数量与历史批次不在此重复维护。

## 状态口径

- `PASS`：当前明确范围有直接、新鲜且层级匹配的证据。
- `PARTIAL`：存在实现或局部证据，但完整旅程、真实数据或目标环境仍有缺口。
- `BLOCKED`：必要输入、决定、权限或环境尚未成立。
- `UNKNOWN`：未取得足以判断当前状态的新鲜证据。

任何一项 `PASS` 都只证明该行范围，不自动提升整站、真实联调或生产状态。

## 总体成熟度与发布候选

**整体产品：`WAITING_PREREQUISITES`**。当前仓库有可继续复核的本地工程资产；D.32 已明确中文交易目标，但交易仍暂停，正式内容、目标数据库、报价转单、外部投递、唯一生产环境和基线发布证据尚未闭环，不能声明整体成熟目标完成。英文完整旅程仍是长期目标，但不自动阻断一个另行批准并限定为中文的候选。

**具体发布候选：`UNKNOWN / 待绑定批准范围与证据`**。D.32 只给出下一交易候选方向，尚未形成可上线候选。Go/No-Go 必须绑定首发能力、`RELEASE_PROFILES` 档位、语言范围、精确版本、目标环境和证据；按 D.34 保留共同基线门禁，高保证附加项只在被选择或适用时执行。

| 验收维度 | 当前状态 | 已知证据边界 | 关闭条件 |
| --- | --- | --- | --- |
| 品牌与正式内容 | `BLOCKED` | 方向、视觉规范、页面构建能力和输入清单存在；历史页面与素材证据不能证明今天可公开使用 | 正式主体、作品、文案、联系资料、媒体权利和 SEO 签认后完成公开页面验收 |
| 公开浏览、选款与咨询 | `PARTIAL` | 路由、页面、合同及本地/隔离测试资产存在；真实作品、正式内容和外部送达未完整验证 | 用获批数据完成游客到后台跟进、客户反馈的真实 API 与权限闭环 |
| 页面装修与模板 | `PARTIAL` | 单一 `PageDocument` / `TemplateDefinitionV2` 模型、独立编辑会话和共享四区外壳实现存在；[唯一标准](../page-builder/template-creation-rules.md)明确创建、按对象微调、检查保存三阶段，七步仅属于创建，并细化四区入口与状态；现有代码与历史检查不证明新增微调清单全部通过 | 按总标准 9.2～9.5 逐类验证微调入口、快捷与非预设值、作用范围、取消/撤销、保存回读、区域联动、异常返回及视图不写入，再核对创建最低质量及复制；模板发布、页面真实内容发布、版本升级、公开 Renderer、并发和受保护草稿分别验收 |
| 后台运营 | `PARTIAL` | 商品、内容、客户、咨询、订单、履约、售后和通知有代码入口；D.33 允许单人 `SUPER_ADMIN` 留痕自审。当前源码与本地目标测试覆盖独立确认、操作者/角色、精确提交 revision、内容哈希、并发保护和发布绑定；本轮新鲜验证及未覆盖层级见 `docs/CURRENT_STATE.md`，不据此推断完整运营或生产通过 | 在目标数据和角色下完成创建、审核、自审留痕或多人复核、发布、跟进、失败恢复和审计连续旅程 |
| 报价、交易与售后 | `BLOCKED / PAUSED` | D.32 已明确中文交易目标，D.2 仍暂停 `commerce` 与真实资金；交易状态机及局部测试资产存在，三报价、本人确认、事务转单、真实渠道和对账尚未形成统一真实闭环 | 完成授权范围内的普通实现；对破坏性 Schema、权限扩大、真实渠道、资金和目标环境取得精确批准，并完成真实数据库、权限、并发、支付、退款和对账验收 |
| 安全、隐私与质量 | `PARTIAL` | 代码合同、守卫、CSRF、隐私和质量门禁资产存在；2026-09-14 当前本地工作树已在 Node 22 完成根测试、lint、typecheck、构建及规则/发布/目标浏览器验证，真实 MySQL 关键套件另行通过；远端与目标环境仍未运行 | 在冻结并推送的候选上取得远端完整 Quality Gate，关闭阻断问题并记录跳过项的责任与边界 |
| 数据库安装与升级 | `PARTIAL` | [2026-09-12 隔离空库验证](../CURRENT_STATE.md#数据库安装验证2026-09-12)保留 53 个迁移和 51→53 演练历史；2026-09-14 当前工作树另在一次性 MySQL 8 空库应用 56 个 migration 并通过六类真实数据库测试。两者都不覆盖任何精确目标库、生产存量数据或真实回退 | 在获批的精确目标库核对备份和 migration ledger，验证存量数据守卫、升级与回退；生产仍需独立授权和验收 |
| 发布与持续运行 | `BLOCKED` | 部署、制品、备份和恢复接口存在；没有唯一目标环境的完整基线证据 | 按 D.34 绑定域名/TLS、Secret、管理员、migration、监控、恢复、不可变制品、私有 SBOM/BuildKit provenance、责任人和回退点；Cosign/SLSA 签名、独立 evidence workflow 与公共透明日志仅在选择高保证层或外部要求适用时追加 |

## 订单、履约与售后后台运营本地闭环（2026-09-12）

- 结论：本次“真实本地后台运营”精确范围为 `PASS`；总体“后台运营”仍为 `PARTIAL`，“报价、交易与售后”仍为 `BLOCKED`，因为本次没有开放客户交易、连接真实支付/退款/物流，也没有验证三报价、对账或任何生产环境。
- 候选与隔离：基于 `041c6e1` 和本次未提交测试增量，在任务专属 MySQL 8 容器、数据库、账号及端口 `33627/35127/45127` 中运行；只使用合成账号、客户、商品、库存、订单和线下收款数据。`RELEASE_PROFILE=lead-generation`，客户交易、支付网关交易和支付网关退款开关均保持关闭；未读取或修改真实环境配置与真实业务数据，未修改并行工作区资产。
- 真实 API 连续旅程：Node `22.23.0` 启动完整 Nest 应用和全局鉴权/校验/异常链；管理员、客服、仓储、编辑与客户身份完成订单查询、线下收款补录、库存占用与履约创建、发货幂等重试、物流异常与送达、签收、售后申请/审核/逆向物流/质检、退款申请/审核/执行失败/重试/完成。最终同一订单为完成与签收，履约已送达，付款部分退款，售后和退款均完成且关联一致；记录 17 类交易事件并覆盖订单、付款、履约、售后、退款操作审计。
- 安全失败路径：未登录请求、客户令牌进入后台、仓储/编辑读取通用订单、客服创建或审核退款、仓储读取退款均被拒绝；跨客户查询为空，跨客户创建或撤销售后返回不存在；非法状态跃迁、重复有效售后、重复线下收款、同幂等键改变退款金额、同退款改变渠道流水均安全失败，失败执行不提前改变退款终态。
- 浏览器与工程门禁：生产构建前端通过真实管理员会话连续回读订单中心、履约中心、售后中心和退款中心，四页无 API 4xx/5xx；既有 `order-manage-capabilities.spec.ts` 5/5 通过。目标 orders/fulfillment/after-sales/refunds 测试 111 项中 109 通过、2 项按显式真实库开关跳过；本次真实 HTTP/MySQL 用例 1/1 通过，既有交易并发/审计回滚真实 MySQL 用例 1/1 通过。服务端类型检查和构建、客户端类型检查和生产构建均在 Node 22 通过；大 chunk 仅保留 Vite 警告。
- 未覆盖：生产部署、真实第三方支付/退款/物流、真实客户与订单、外部对账和通知送达均未执行。本次证据只关闭指定后台本地闭环，不能替代这些门禁。

## 模板创建分工局部验收（2026-09-11）

- 候选：`7fa51d2` 基线上的本次未提交工作区修改；环境为 `127.0.0.1:5173` 真实开发前端与 `3000` 本地 API，使用现有管理员会话，仅新建本任务的“临时验收-布局与图片分工”草稿。
- 已通过：布局步骤的构图、图片组合、固定排列和线框示意；图片步骤的逐图形状、比例、填充及选中提示；4:3 画幅下圆形与 3:4 双图并排、横纵排列往返、风格变更保留明确设置、自定义名称与 Logo 往返、结构撤回、背景图约束、390 px 窄屏预览及取消不改变已保存草稿。
- 真实保存首次被旧 API 编译合同拒绝；构建并重启本地 API 后，同一草稿创建返回 201，刷新重开草稿返回 200。回读的画布为 1200×900、`mediaArrangement=row`，圆形和 3:4/contain 均保留，`defaultContent` 为空。样例仅保存草稿，未发布。
- 检查：29 项生成器测试、固定排列与历史兼容纯测试、线框坐标纯测试、客户端类型检查、目标 ESLint、服务端构建、模板合同一致性检查通过。已同步受影响浏览器测试路径；本次未运行完整 Mock 浏览器套件。
- 本地证据：`.codex-tmp/template-flow-redesign/` 中的界面截图与 `save-reopen-evidence.json`。该结论仅覆盖本次创建分工及真实保存回读；模板发布、页面实例发布、公开 Renderer 和完整微调矩阵仍按上表分别验收，总体状态保持 `PARTIAL`。
- 同日状态保留复核与修复：浏览器确认并修正了组合历史覆盖主图最新比例、仅应用风格推荐清除 Logo 独立圆角、用途/组合切换恢复过期继承圆角的问题；确认页补主题/背景/文字实际色值。历史组合按对象身份同步最新设置，新图片分配避开历史身份；显式重建图片推荐清理旧组合历史。
- 本轮真实复验：主图 3:4/contain 在组合往返后保留；Logo 27 px 在只应用风格推荐后保留；未显式圆角跟随高端风格为 0，显式 13 px 与 Logo 27 px 经无图片→自定义→恢复组合后仍保留；切换用途的新推荐图片继承当前风格。样例“临时验收-方案设置保留”返回 201 保存、刷新后 200 回读，确认画布 1200×900 与上述独立值。浅色/深色实际配色摘要与预览同步，390 px 下无页面横向溢出；自定义原生颜色选择器未作本轮自动化验收。证据位于 `.codex-tmp/template-flow-state-fixes/`。两份新增状态保留纯测试、29 项生成器测试、客户端类型检查及目标 ESLint 通过；未发布，未扩展为完整七步组合或公开前端验收。

## 模板跨步骤设置保留修复（2026-09-12）

- 范围：创建向导的 Logo 显隐恢复、撤回图片结构、背景角色期间风格更新，以及图片圆角非法输入。保留既有工作区修改；不修改合同、持久化、历史模板或发布流程。
- 真实开发浏览器验收：在独立的 `127.0.0.1:5173/admin/editor/catalog` 页创建临时方案，Logo 27 px 经关闭/开启后保持；主图改为 3:4 后撤回结构仍保持；圆角 27 px 经背景角色→高端风格→普通图片后保持。最终确认页显示画布 1200×900（4:3）、主图 3:4/27 px、Logo 1:1/27 px，同时整体风格圆角为 0。仅操作内存向导，未创建保存记录、未发布，临时页面已关闭。
- 输入验收：201、空白、1.5 均保留原始文本并就近报错；切换图片和返回步骤后错误不丢失，前往/返回确认被阻止，修正为 27 后恢复可操作。预览注明保留上次有效圆角。390 px 窄屏无页面横向溢出，错误提示可见。
- 目标检查：29 项生成器测试、3 项圆角输入纯测试、图片历史与用途继承断言、客户端类型检查及目标 ESLint 通过。既有浏览器测试的圆角定位已同步到文本输入；未运行完整 Mock 浏览器套件。
- 截图：`.codex-tmp/template-settings-repair/confirmed-settings.png`、`invalid-radius-narrow.png`。此条只关闭上述四项创建会话问题，不提升真实保存、模板/页面发布或生产状态。

## 证据入口

- 当前实现与阻断：`docs/CURRENT_STATE.md`。
- 稳定用户旅程：`docs/acceptance/end-to-end-flow-matrix.md`。
- 动态工作包：`docs/AI_PROGRAM_LEDGER.md`。
- 正式内容输入：`docs/acceptance/V1_CONTENT_PRODUCTION_INPUT_CHECKLIST.md`。
- 页面装修：`docs/page-builder/README.md`。
- 发布与恢复：`docs/PRODUCTION_RELEASE_RUNBOOK.md`。

## 更新规则

只有取得新的当前证据或明确批准决定时才更新对应行。每次更新必须记录版本、环境、身份、数据来源、通过范围和未覆盖项；不得复制大段测试日志，也不得把归档报告恢复成当前状态。
