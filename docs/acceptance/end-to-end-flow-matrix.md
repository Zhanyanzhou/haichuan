# HC-MASTER-ACCEPTANCE-10: 端到端流程矩阵

> 最后核对：2026-08-23。状态分为“已验证”“仅代码/隔离测试”“未验证/未完成”“安全冻结”；不得把代码、Mock、路由或页面存在写成真实闭环。
> `🎯 已确认目标、尚未实现` 表示产品规则已经确认，但代码、Schema、迁移、后台能力和真实数据验收尚未完成。

## 商品发布链

| 步骤 | 状态 | 证据 |
|---|---|---|
| 后台新建/编辑商品 | 🧪 仅代码/隔离测试 | ProductEditor 与 API/DTO 已接线；真实开发库 CRUD 未验收 |
| SKU 价格与库存策略 | 🧪 目标测试 | SKU 价格事实源、STANDARD/SINGLE_UNIT 规则有目标测试；migration 未确认执行 |
| 草稿、发布门禁、下架 | 🧪 目标测试 | 状态和门禁代码通过目标测试；运行后端可能仍是旧 `dist` |
| `/catalog` 公开目录 | ✅ 当前只读结果 | 当前仅 3 条 `DISPLAY_ONLY` 测试商品，不足以验证直购、售罄或单件库存 |
| 搜索/筛选/排序 | 🧪 仅代码/隔离测试 | 统一位于 `/catalog`；`/search` 只兼容重定向，不是独立页面 |
| `/products` | ✅ 隔离浏览器行为 | 品牌展陈，不承担商品结果墙或交易 |
| 商品详情与下架隐藏 | 🧪 仅代码/隔离测试 | 真实状态矩阵未完成 |
| 所有详情对应行动路径 | 🎯 已确认目标、尚未实现 | 五种 `SalesMode` 分别对应品牌顾问咨询（不直接下单）、选款、预约、直购或高级定制报价；`/catalog` 仍是唯一搜索筛选中心 |
| **完整闭环** | ❌ 未完成 | 需确认迁移与运行版本，建立代表性开发数据后完成后台写入→发布→公开回显 |

## 页面发布链（Puck PageDocument）

| 步骤 | 状态 | 证据 |
|---|---|---|
| 23 模板合同与 Renderer 注册 | ✅ 目标检查 | 单一机器合同、双端生成物、根构图与比例派生检查通过 |
| 编辑器工作台 | 🧪 仅代码/隔离测试 | 入口与状态代码存在；本轮未登录真实后台 |
| 保存、预检、发布 | 🧪 仅代码/静态测试 | API 与服务端复检链存在；未执行真实 CMS 操作 |
| 公开已发布首页 | ✅ 本地只读检查 | 当前 `id=3/version=38`；含 `AURÉLIA PARIS` 素材，内容验收未通过 |
| CMS 中性替换 | ❌ 尚未执行 | 已批准保留原文件和 revision 的可逆替换流程，不能写成已整改 |
| 未发布 FallbackHome | 🧪 兼容兜底 | 只证明不白屏，不证明内容、授权或视觉通过 |
| 版本历史 | 🧪 仅代码/静态测试 | revision 可读；恢复缺少乐观并发控制，不能作为多人安全回退 |
| **完整闭环** | ❌ 未完成 | 需草稿保存、刷新回显、双端预览、预检、发布、公开回显及版本记录的真实浏览器验收 |

## 预约咨询链

| 步骤 | 状态 | 证据 |
|---|---|---|
| 前台表单与隐私同意 | 🧪 代码/隔离测试 | 可访问性与校验有目标测试 |
| 提交、限流与 DTO | 🧪 代码/静态测试 | 尚未对已确认开发库写入测试线索 |
| 后台聚合与状态流转 | 🧪 仅代码 | 页面/API 存在，不等于真实数据回显与权限路径已验收 |
| **完整闭环** | ❌ 未验证 | 需前台提交→后台读取→分配/跟进→失败态的真实开发数据验收 |

## 客户选款链

| 步骤 | 状态 | 证据 |
|---|---|---|
| 加入选款与隐私同意 | 🧪 代码/隔离测试 | Catalog 选款托盘与隐私路径存在 |
| 提交、快照与后端校验 | 🧪 代码/静态测试 | 尚未对已确认开发库写入代表性选款数据 |
| 后台管理与状态流转 | 🧪 仅代码 | 真实回显、权限和失败态未联调 |
| **完整闭环** | ❌ 未验证 | 需 `/catalog` 提交→后台读取/处理→公开/客户回显的真实开发数据验收 |

## 客户分层、合作申请与客户专属价格链

| 步骤 | 状态 | 证据 |
|---|---|---|
| `RETAIL` 普通客户 | 🎯 已确认目标、尚未实现 | 只进入公开零售价或高级定制报价流程，不读取客户专属双克价 |
| “我的账户”合作申请 | 🎯 已确认目标、尚未实现 | 唯一入口及公司/商户名称、联系人、手机、地区、主营业务、预计月用量、营业执照/资质、合作说明尚未完整落地 |
| 人工审核与状态流转 | 🎯 已确认目标、尚未实现 | 申请中/补资料/驳回不得获得合作价或合作下单；只有人工审核 `APPROVED` 才授权 |
| 合作商红蜡/紫蜡克价 | 🎯 已确认目标、尚未实现 | 仅 `APPROVED` 本人可见；有效专属双克价优先，否则使用系统默认双克价，并覆盖客户隔离、生效区间、调价历史和无可用价格失败态 |
| 合作订单创建 | 🎯 已确认目标、尚未实现 | 仅 `APPROVED` 可创建，并固化成交价格类型、克价、生效区间和订单快照 |
| `SUSPENDED` 暂停 | 🎯 已确认目标、尚未实现 | 立即拒绝新价格、新报价和新合作订单，同时保留本人暂停前历史只读访问 |
| 后台客户经营管理 | 🎯 已确认目标、尚未实现 | 目标聚合申请审核、客户专属双克价、生效区间、调价历史、普通/定制/合作订单和审计 |
| **完整闭环** | ❌ 未完成 | 需审批 Schema/权限/交易变更后，以代表性客户和真实开发数据验证越权、并发、调价与暂停路径 |

## 数据采集链（P0-C 设计决定：默认关闭）

| 步骤 | 状态 | 证据 |
|---|---|---|
| trackPageView / trackSearch 等 | 🧊 no-op | useAnalytics ANALYTICS_ENABLED=false |
| _asid 匿名标识 | 🧊 不创建 | 模块级不写 localStorage |
| /analytics/track 请求 | 🧊 不发送 | send 函数注释停用 |
| **状态** | 🧊 安全冻结 | 未来开启需先完成隐私偏好、授权和真实环境验收 |

## 交易链（P0-B/D 安全冻结）

| 链路 | 状态 | 证据 |
|---|---|---|
| 当前运行开关 | 🧊 已验证关闭 | 本地 API 返回 commerce/cart/payment 全 false |
| 购物车、结算、支付 | 🧊 安全冻结 | 关闭态和 Guard 有目标测试；没有真实交易开放证据 |
| 后台收款审核 | 🧪 仅代码/隔离测试 | 页面与 API 路径存在，真实凭证、权限和库存实扣未联调 |
| 履约、退款、售后 | 🧪 仅代码/隔离测试 | 页面与服务代码存在，无连续真实订单数据验收 |
| 报价、异常订单、交易总览 | 🧪 仅代码/隔离测试 | 不得因页面存在宣称经营闭环完成 |
| **状态** | ❌ 真实闭环未完成 | 支付和生产继续关闭；先完成迁移、运行版本、代表性开发数据和权限/失败态验收 |

## 公开信息真实性链（P0-C，2026-08-13）

| 步骤 | 状态 | 证据 |
|---|---|---|
| SiteSettings 后台配置 | 🧪 代码接线 | 联系字段待运营填入并公开复核 |
| 公开设置接口与空值降级 | 🧪 代码/隔离测试 | 不能代替真实运营信息确认 |
| 隐私说明与入口 | 🧪 代码/隔离测试 | 生产内容未核验 |
| 首页品牌素材 | ❌ 未通过 | 当前发布首页仍含 `AURÉLIA PARIS`；已批准 CMS 可逆替换但尚未执行 |
| **完整闭环** | ❌ 未完成 | 需真实内容、素材授权、双端公开页面和跳转复核 |
