# 海川珠宝 — 4000+ 商品导入产品决策表（PRODUCT_IMPORT_DECISIONS）

> 本文件是 `docs/PRODUCT_DATA_CONTRACT.md` 第 13/14 节【待产品决策】事项的**可执行决策表**。
> 用途：产品负责人逐项勾选/填空后，本文件即成为 4000+ 商品正式导入的**业务输入基线**。
> 每项给出：① 代码事实依据 ② 可选方案与利弊 ③ **AI 推荐（仅供参考，非决策）** ④ 决策栏（留空待填）。
> 填写后请标注决策人与日期；未填决策的项，导入不得开始。
> 最近核对：2026-08-12（基于当前工作区代码）。

## ✅ 决策状态（2026-08-12）

**所有决策项已由产品负责人确认：「采纳全部 AI 推荐」。** 下文每项的「AI 推荐」即为落定决策，文末汇总表已填入落定值与决策人/日期。

**仍需业务提供具体输入（不阻塞字段模板设计，但阻塞实际导入）的 3 个子项：**
- **D-4 映射表**：`code → categoryId` 的权威数据（人工梳理 / AI 辅助 / 业务给定，方法待定）。
- **D-6 多实物**：同 `CODE` 多实物（如 ATP1055 圆石群镶 vs 珐琅）的拆分规则待定。
- **D-13 OSS**：上线时间表决定「本地试跑再切 OSS」还是「导入即 OSS」。

> 字段模板与导入输入规范见 `docs/PRODUCT_IMPORT_SPEC.md`（基于本表落定决策生成）。

## 填写说明

- `[ ]` 勾选；`____` 填空。决策栏格式：`决策：A / 决策人：___ / 日期：____`。
- **AI 推荐基于"公网上线、安全、数据正确、稳定、可维护"导向，并尽量与现有代码行为一致、回避未验证机制**。推荐仅供参考，最终以业务判断为准。
- 实施类（涉及 Schema/代码/脚本改动）均属 B 类，决策后仍须按 `WORKFLOW.md` 先出方案再实施。

## 决策依赖顺序

```
D-1 商品类型/salesMode ──┬─→ D-7 价格来源 ──→ D-10 priceMin/Max
                        ├─→ D-2 库存写法 ──→ D-3 多仓
                        └─→ D-11 发布策略
D-4 分类映射（独立，必须先有）
D-5 图片角度 / D-6 同款聚合（独立）
D-8/D-9 Json 空值（独立）
D-12 商品名来源（独立）
D-13/D-14/D-15（可延后）
```

## 优先级分组

- **第一组 · 阻塞型**（不定则导入主数据表无法设计）：D-1、D-4、D-5、D-6、D-7、D-12
- **第二组 · 约定型**（影响导入字段格式）：D-8、D-9、D-10、D-11
- **第三组 · 可延后**（不阻塞首轮导入）：D-2、D-3、D-13、D-14、D-15

---

# 第一组 · 阻塞型决策

## D-1 商品类型与 salesMode（最关键，决定整条链）

**代码事实**：项目当前不开放真实支付（`docs/PROJECT_GUARDRAILS.md`）；`DIRECT_PURCHASE` 受 `featureFlags.commerceEnabled=false` 控制、schema 注释"暂未开放"；`salesMode` 后端无业务分支，与是否需 SKU/库存无耦合；现有 `create-products.mjs` 用 `SELECTION`，导入的是无 SKU/无库存/无价格的"空壳商品"。

**选项**：
- [ ] **A. B 类 · 展示**（`salesMode=DISPLAY_ONLY`）：纯展示，不询价不下单，无 SKU 无库存。
- [ ] **B. B 类 · 选款咨询**（`salesMode=SELECTION`）：展示 + 走选款咨询/线索，无 SKU 无库存（与现有"选款咨询"模块、现有导入脚本一致）。
- [ ] **C. B 类 · 预约/定制**（`salesMode=APPOINTMENT` 或 `CUSTOM_INQUIRY`）：预约看货或定制咨询。
- [ ] **D. A 类 · 可销售**（`salesMode=DIRECT_PURCHASE`）：需 SKU+库存+价格，**当前未开放**，需先完成电商闭环（支付/订单/库存架构）验证。

**AI 推荐**：**B（`SELECTION`）** —— 与项目"展示+选款咨询"定位、现有脚本、现有 `selection-inquiry` 业务模块完全一致；不引入未验证的支付/交易闭环；可平滑承接现有 4000+ 数据。若业务定位是纯品牌展示则选 A。

**决策**：____ 决策人：____ 日期：____

---

## D-4 分类主数据与 code→categoryId 映射

**代码事实**：`Product.categoryId` 必填、强校验、不自动建分类；现有三套分类并存且互不对应——`seed.ts`(真实 int id, 3 级) / `catalogData.ts`(mock string id, 2 级, 仅客户端) / `homeCampaign.ts`(硬编码 6/17)；服务层**只支持创建 1/2/3 级**（第 4 级代码无法创建）；现有脚本硬编码 `categoryId=1`/`9`。

**选项**：
- [ ] **A. 以 `seed.ts` 体系为唯一真实分类，限 3 级**，提供一份 `code → categoryId` 权威映射表作为导入源；`catalogData.ts` 的分类仅作 mock 保留、不参与导入。
- [ ] **B. 重定分类主数据**（新增/重组分类树，可能需要 4 级——需先改服务层）。
- [ ] **C. 导入全部先进"未分类/待分类"临时分类**，后续人工/AI 二次分类。

**AI 推荐**：**A** —— 复用现有 seed 体系零迁移成本；3 级已覆盖"手镯/吊坠/戒指/耳饰 → 子类 → 子子类"；映射表是导入的必备输入。**关键待定：映射表（每个 ATP 货号属于哪个分类）由谁、用什么方法产出**（人工梳理 / AI 分类辅助 / 业务给定）。

**子决策（映射表来源）**：____ 决策人：____ 日期：____

**决策**：____ 决策人：____ 日期：____

---

## D-5 图片角度方案（背面图 BACK）

**代码事实**：`ImageType` 枚举 = `FRONT/SIDE/TOP/DETAIL/WEARING`，**无 `BACK`**；实际图片文件命名大量 `*_BACK_*`（背面）；`batch-upload.mjs` 无"背面"分支 → BACK 落 `DETAIL`；`batch-upload-images.ts` 更把所有图硬编码 `FRONT`；`type` 还被 `setPrimaryImage` 当主图标记用（语义双关）。

**选项**：
- [ ] **A. 新增 `BACK` 枚举值**（需 Schema 迁移 + `getImageType` 加"背面→BACK"分支 + 前端展示适配）：保留正反面视角语义。
- [ ] **B. BACK 统一归 `DETAIL`**，靠 `sortOrder` + 文件名约定区分（不动 Schema）：成本低，但前台无法结构化区分正反面。
- [ ] **C. 只保留正面图，背面图不入库**。

**AI 推荐**：**A** —— 珠宝正反面都是核心展示视角，丢失语义会导致前台展示错乱；Schema 改动小（仅加枚举值），脚本改动一行。属 B 类需 Migration 确认，但收益明确。

**决策**：____ 决策人：____ 日期：____

---

## D-6 同款聚合策略（避免 ATP#### 被拆成多商品）

**代码事实**：`batch-upload.mjs` 的 `variantKey` 保留了文件名中间的 `_FRONT_/_BACK_/_SIDE_` 段，导致同一 `ATP103` 的三张图被拆成 3 个变体分组 → 被 `create-products.mjs` 创建为 3 个商品（`ATP103`、`ATP103-2`、`ATP103-3`）；事后靠 `server/prisma/merge-variants.ts` / `rename-codes.ts` 清理（脚本存在即证据）；同 CODE 多实物（如 ATP1055 有不同描述）合并时还会跨实物混图。

**选项**：
- [ ] **A. 修 `batch-upload.mjs` 的 `variantKey`**：分组时一并去掉 `_FRONT_|_BACK_|_SIDE_|_TOP_|正面|背面|侧面` 等角度段，让同 `ATP####` 所有图归一组（正/背/侧合并为同一商品的多图）。
- [ ] **B. 预先提供"货号清单"作为分组主键**，脚本只按清单聚合，不在文件名里推断。
- [ ] **C. 维持现状 + 继续用 merge 脚本事后清理**（风险高，不推荐）。

**AI 推荐**：**A + B 结合** —— 以业务提供的"货号清单"为主键（B），脚本按货号聚合所有图（A 的去角度逻辑）。同时**必须先定义"同 CODE 多实物"如何处理**（拆成 `ATP1055-A`/`ATP1055-B` 还是只保留一个）。

**子决策（同 CODE 多实物处理）**：____ 决策人：____ 日期：____

**决策**：____ 决策人：____ 日期：____

---

## D-7 价格数据来源与多 SKU 规则

**代码事实**：现有脚本完全不导入价格（默认 `price=0`）；金价调价公式 `round((goldWeight×goldPrice×1.05+craftFee)/10)*10`，**仅同步 `goldWeight>0` 的商品/SKU**；`goldWeight=0` 的价格凝固；`Product.price` 是列表展示价、`SKU.price` 是详情下单价，二者无强约束。

**选项**（价格来源）：
- [ ] **A. 主数据表提供 `goldWeight/craftFee/price` 三列**，导入直接写 `Product.price`（B 类展示用一口价，无 SKU）。
- [ ] **B. 只导 `goldWeight/craftFee`，`price` 由金价引擎事后批量计算**。
- [ ] **C. 无价格**（`price=0`，纯展示"询价"）。

**选项**（多 SKU 价格规则，仅当 D-1 选了需 SKU 的模式）：
- [ ] **R1. `Product.price = min(SKU.price)`**
- [ ] **R2. `Product.price = 主 SKU（第一个/标主）price`**
- [ ] **R3. 不强制，人工保证一致**

**AI 推荐**：**A**（导一口价到 `Product.price`，不建 SKU，与 D-1 推荐 B 一致）；多 SKU 规则 R1（列表展示最低价是常规电商做法，但 B 类展示场景下不建 SKU 即不涉及）。

**决策**：____ 决策人：____ 日期：____

---

## D-12 商品名与内容来源

**代码事实**：现有商品名由 `batch-upload.mjs` 从文件名截取得来，产生残缺名（实测 `"珐琅"`、`"ATP1056"`）；根目录 `_names.json`（700+ 条 `ATP####→中文名` 字典）**未被任何代码引用**，是孤立资产；`shortDescription`/`description` 现脚本完全不导入。

**选项**：
- [ ] **A. 主数据表为名/内容唯一来源**：以 `_names.json` 作商品名基础（人工校对补全），`shortDescription`/`description` 由人工或 AI 文案另行提供。
- [ ] **B. 继续从文件名推断商品名**（质量低，不推荐）。
- [ ] **C. 商品名留空/用货号占位**，后续人工逐个补。

**AI 推荐**：**A** —— 复用现成的 `_names.json`（700+ 条已有人工中文名），人工校对后作为权威名来源；简介/描述作为后续任务单独补，不在首轮导入强制。

**决策**：____ 决策人：____ 日期：____

---

# 第二组 · 约定型决策

## D-8 gemInfo 空值与形状约定

**代码事实**：schema `Json?`，后端 DTO/Service 全 `any` 无校验；**形状约束只在前端**：`{type,carat,clarity,color,cut,quantity}`，`type` 英文小写枚举（`diamond/ruby/sapphire/emerald/jade/pearl/other`）；编辑器"未填"写 `null`。

**选项**：
- [ ] **A. 统一空值写 `null`**，有值时写完整对象（与编辑器现状一致）。
- [ ] **B. 空值不写字段**（导入时缺省）。
- [ ] **C. 空值写 `{}`**。

**AI 推荐**：**A** —— 与 `ProductEditor` 现状完全一致，导入数据与人工编辑数据行为统一，零额外适配。导入时 `type` 必须用英文小写枚举值，不能写中文。

**决策**：____ 决策人：____ 日期：____

---

## D-9 craftTechnique 空值与形状约定

**代码事实**：schema `Json?`，后端 `any`；**前端是 `string[]`**（`Select mode="tags"`，预设 `3D硬金/古法金/花丝/錾刻/抛光/喷砂/镶嵌/镂空/浮雕/拉丝/磨砂`）；编辑器"未填"写 `[]`；`ProductDetail` 用 `.map` 渲染——**若导入写字符串或对象会运行时崩溃**。

**选项**：
- [ ] **A. 统一写 `string[]`**，无值写 `[]`（与编辑器现状一致）。
- [ ] **B. 无值写 `null`**（与 gemInfo 不一致，且前端 `|| []` 兜底虽不崩但风格分裂）。

**AI 推荐**：**A** —— 必须是数组（否则详情页崩）；空值 `[]` 与编辑器一致。导入词表建议从预设 11 个里选，允许补充。

**决策**：____ 决策人：____ 日期：____

---

## D-10 priceMin / priceMax 处理

**代码事实**：死字段——后端 `findAll` 不按其过滤，前端编辑器无写入 UI，仅 `clone` 写、仅 `priceMin` 在 `price=0` 时作展示兜底；金价引擎不同步它们。

**选项**：
- [ ] **A. 导入不填（保持默认 `0`）**，本轮不废弃字段。
- [ ] **B. 启用为价格区间**（需补 `findAll` 过滤 + 编辑器 UI）。
- [ ] **C. 废弃字段**（需 Schema 迁移）。

**AI 推荐**：**A** —— 导入不填、不删字段，零风险；是否启用/废弃作为独立后续决策。

**决策**：____ 决策人：____ 日期：____

---

## D-11 导入后发布策略

**代码事实**：`create` 直接接受 `status:'PUBLISHED'` **不校验 `price>0`、不写 `publishedAt`**（现有脚本正是这样，会产生 0 元已上架商品）；只有 `updateStatus` 路径强制 `price>0` 门禁。

**选项**：
- [ ] **A. 导入默认 `status=DRAFT`**，发布统一走 `updateStatus`（带 price>0 门禁 + 写 `publishedAt`）。
- [ ] **B. 导入直接 `status=PUBLISHED`**（绕过门禁，快但有 0 元上架风险）。
- [ ] **C. 视价格决定**：有价直接发布，无价留 DRAFT。

**AI 推荐**：**A** —— 最稳妥；导入后由运营批量"发布"，天然拦截 0 元商品，且 `publishedAt` 语义正确。是否把门禁收口到 `create`/`update` 代码层，作为独立 B 类改进（见 D-15 之外的代码改进项）。

**决策**：____ 决策人：____ 日期：____

---

# 第三组 · 可延后决策（不阻塞首轮导入）

## D-2 库存写入策略

**代码事实**：`ProductSKU.stock` 标 `@deprecated`（注释称走 `InventoryService`），但 `InventoryService` 无消费者；`products.service`/`orders.service` 仍直读写 `ProductSKU.stock`；双轨互不同步（与 `DECISIONS.md` D.1 同源）。

**选项**：
- [ ] **A. 首轮导入不建 SKU、不写库存**（与 D-1 推荐 B 类一致，回避双轨）。
- [ ] **B. 建 SKU 并写 `ProductSKU.stock`**。
- [ ] **C. 建 SKU + `Inventory`（多仓）**。

**AI 推荐**：**A** —— 库存架构未定（DECISIONS D.1 未决）前，导入不碰库存最安全；待 A 类电商上线决策时一并定。

**决策**：____ 决策人：____ 日期：____

---

## D-3 默认仓库与多仓启用

**代码事实**：`Warehouse` 类型 `SHOWROOM/FACTORY/STORE`；`Inventory` 按 `(skuId, warehouseId)` 维度；当前库存主要落 `ProductSKU.stock`，多仓未实际启用（与 `DECISIONS.md` D.9 同源）。

**选项**：
- [ ] **A. 暂不启用多仓**（与 D-2 推荐 A 一致）。 [ ] **B. 单仓起步**（指定一个默认仓库）。 [ ] **C. 多仓**。

**AI 推荐**：**A** —— 依赖 D-2，暂不启用。

**决策**：____ 决策人：____ 日期：____

---

## D-13 图片存储 / OSS / CDN / 缩略图

**代码事实**：上传走本地 `uploads/YYYY/MM/DD/uuid.ext`；`ali-oss` 依赖在 package.json 但**无业务代码 import**；`.env` 无 OSS 配置；无缩略图/裁切（列表页直拉原图）；4000×3×~3MB ≈ **30-40GB** PNG。

**选项**：
- [ ] **A. 导入先用本地存储试跑，正式上线前切 OSS+CDN+缩略图**。
- [ ] **B. 导入即启用 OSS**（需先配 `.env` + 改 `upload.service` + 切换存储后端）。
- [ ] **C. 保持本地**（不适合公网，不推荐）。

**AI 推荐**：**A 或 B 视上线时间表定** —— 公网上线**必须** OSS+CDN+缩略图（否则列表页性能不可用、本地盘容量不足）。若上线紧迫，直接 B；若仍有窗口，A 先跑通数据再切。**注意：30-40GB 对开发机本地也是负担，建议尽早至少切 OSS 存储层。**

**决策**：____ 决策人：____ 日期：____

---

## D-14 金价调价系数与 AUTO 行情源

**代码事实**：调价系数 `1.05` 硬编码；AUTO 定时任务是空壳（`fetchAndUpdateGoldPrice` 只打 warn）；手动调价绕过 `ProductsService`（不触发 SSE）；`currentPrice/getCurrentPrice` 是死代码（与 `DECISIONS.md` D.10 同源）。

**选项**：
- [ ] **A. 首轮不涉及**（导入不依赖调价；系数/AUTO 维持现状）。
- [ ] **B. 系数参数化**（改为配置）。
- [ ] **C. 接入 AUTO 行情源**。

**AI 推荐**：**A** —— 与导入解耦，后续独立决策。

**决策**：____ 决策人：____ 日期：____

---

## D-15 AI 分类是否回写 Product.categoryId

**代码事实**：`ai-classify` 的 `confirmClassification` 只更新 `AIClassifyRecord`，**不回写 `Product.categoryId`**，且记录不带 `productId`；AI 预测与商品正式分类当前无自动衔接。

**选项**：
- [ ] **A. 导入阶段不依赖 AI 自动回写**，AI 分类仅作 D-4 映射表生成的辅助（人工确认后批量写入）。
- [ ] **B. 改代码让 `confirmClassification` 回写 `categoryId` + 记录带 `productId`**（B 类）。

**AI 推荐**：**A** —— 首轮用 AI 辅助生成映射表、人工把关，避免未经验证的自动写入污染 4000+ 商品分类；代码回写作为独立 B 类改进。

**决策**：____ 决策人：____ 日期：____

---

# 汇总：导入启动前必须完成的决策

按依赖顺序，**以下决策栏全部填写后，导入主数据表才能设计、导入才能启动**：

| 序号 | 决策项 | 优先级 | AI 推荐 | 落定决策 | 决策人/日期 |
| --- | --- | --- | --- | --- | --- |
| D-1 | 商品类型/salesMode | 阻塞 | B（SELECTION） | **B 类 `SELECTION`** | 产品负责人 / 2026-08-12 |
| D-4 | 分类主数据+映射来源 | 阻塞 | A（seed 体系 3 级） | **seed.ts 体系，限 3 级** ｜ ⚠️映射表数据待业务提供 | 产品负责人 / 2026-08-12 |
| D-5 | 图片角度 BACK | 阻塞 | A（新增枚举） | **新增 `BACK` 枚举**（需 Schema 迁移，B 类） | 产品负责人 / 2026-08-12 |
| D-6 | 同款聚合+多实物处理 | 阻塞 | A+B 结合 | **货号清单主键 + 去角度聚合** ｜ ⚠️多实物拆分规则待业务定 | 产品负责人 / 2026-08-12 |
| D-7 | 价格来源+SKU 规则 | 阻塞 | A（一口价无 SKU） | **一口价导 `Product.price`，不建 SKU** | 产品负责人 / 2026-08-12 |
| D-12 | 商品名/内容来源 | 阻塞 | A（_names.json 校对） | **`_names.json` 为基础 + 人工校对** | 产品负责人 / 2026-08-12 |
| D-8 | gemInfo 空值 | 约定 | A（null） | **空 = `null`** | 产品负责人 / 2026-08-12 |
| D-9 | craftTechnique 空值 | 约定 | A（[]） | **空 = `[]`**（必须数组） | 产品负责人 / 2026-08-12 |
| D-10 | priceMin/Max | 约定 | A（不填） | **不填（默认 0），不废弃字段** | 产品负责人 / 2026-08-12 |
| D-11 | 发布策略 | 约定 | A（DRAFT） | **导入默认 `DRAFT`，发布走 `updateStatus`** | 产品负责人 / 2026-08-12 |
| D-2 | 库存写法 | 可延后 | A（不建） | **首轮不建 SKU/库存** | 产品负责人 / 2026-08-12 |
| D-3 | 多仓 | 可延后 | A（不启用） | **不启用多仓** | 产品负责人 / 2026-08-12 |
| D-13 | OSS/CDN | 可延后 | A 或 B | **公网须 OSS+CDN+缩略图** ｜ ⚠️本地试跑 vs 直接 OSS 待上线时间表 | 产品负责人 / 2026-08-12 |
| D-14 | 调价/AUTO | 可延后 | A（不涉及） | **首轮不涉及**（系数/AUTO 维持现状） | 产品负责人 / 2026-08-12 |
| D-15 | AI 分类回写 | 可延后 | A（不自动回写） | **不自动回写**，AI 仅作映射表辅助 | 产品负责人 / 2026-08-12 |

> 首轮导入执行还需：① D-4 映射表数据；② D-6 多实物拆分规则；③ D-13 OSS 时间表；④ 真实材质/金重/工费/售价数据。这些由业务/产品提供后，导入方可启动。

> 本文件只承载产品决策，不含实现。决策完成后，下一步是 B 线技术设计（导入主数据表字段模板 + 脚本修复方案），均属 B 类需确认。
