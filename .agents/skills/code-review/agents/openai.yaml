interface:
  display_name: "Deep Code Review"
  short_description: "Lineage-aware, product-grounded deep review"
  default_prompt: "Use $code-review for an evidence-based read-only review of the requested scope, returning findings to any already authorized implementation and verification in the same task."

policy:
  allow_implicit_invocation: false
