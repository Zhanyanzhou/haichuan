# 统一换行策略：
# - 文本文件入库归一化为 LF，避免 Windows/ Linux 检出差异；
# - 容器内（mysql backup 容器 / CI bash）直接执行的脚本强制 LF，防止 CRLF 导致 bash 语法错误；
# - Windows 原生批处理与 PowerShell 保持 CRLF；
# - 二进制文件不做任何转换。
* text=auto
*.sh text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.svg text
*.woff binary
*.woff2 binary
*.gz binary
*.tar binary
